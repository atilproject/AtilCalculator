# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
.env.*.local
*.env

# Claude Code
.claude/cache/
.claude/logs/

# Agent scratch worktrees (per TD-009 — per-agent cwd isolation;
# atilcalc-{role}-{task}/ is the convention; nested worktrees are
# architect's local working area, never part of the project tree).
atilcalc-*/

# Logs
logs/
*.log

# OS
Thumbs.db

# Build outputs
*.tsbuildinfo
coverage/
.nyc_output/

# Local data
*.sqlite
*.db

# Local environment files (contain secrets)
.dev-studio-env
.env
.env.local
*.token

# Tmux bootstrap (auto-generated by scripts/dev-studio-start.sh)
scripts/.tmux-bootstrap/

# Template cache (auto-generated by scripts/dev-studio-init.sh; mirror of
# pre-render .tmpl sources for idempotent re-render per ADR-0050 §C9).
# Working tree stays "no .tmpl present" so post-init smoke tests pass;
# .dev-studio/.tmpl-cache/ preserves the source-of-truth for re-runs.
.dev-studio/


# --- Faz 3: rendered template outputs ------------------------------------
# These are generated by scripts/dev-studio-init.sh from their .tmpl source.
# Run init after every fresh clone. Manual edits to these files are lost
# on re-render — edit the corresponding .tmpl instead.
CODEOWNERS
# NOTE (d853): .github/ISSUE_TEMPLATE/config.yml is intentionally NOT
# gitignored — it must be tracked so the canary mirror (`git clone main`)
# carries GitHub's issue chooser config. Was previously classified as a
# Faz 3 rendered output; upstream .gitignore.tmpl in
# atilproject/dev-studio-template should mirror this exception (deferred).
.claude/commands/standup.md
.claude/commands/sprint-start.md
.claude/agents/*.md
scripts/install/systemd/dev-studio-watcher@.service
scripts/install/systemd/dev-studio-watcher-reload.service
scripts/install/systemd/dev-studio-watcher-reload.path
systemd/dev-studio-health.service
# --- Faz 4: BÖL dosyaları rendered outputs (idempotent, edit the .tmpl instead) ---
README.md
.claude/CLAUDE.md
CLAUDE.md

# Python venv (Sprint 1 STORY-002)
.venv/
