# Tech Debt Log

Maintained by @architect per `.claude/CLAUDE.md` §"Things agents must NEVER do" (no "we'll fix it later" without a ticket).

## Active items

| ID | Description | Introduced in | Severity | Payoff trigger | Owner |
|----|-------------|---------------|----------|----------------|-------|
| TD-001 | Stale-cc self-release breaks 4-cat invariant — removing `cc:<self>` while it's the only `cc:*` causes CI label-check failure (5+ observed instances: PR #5, #7, #8, #13, #33, plus recurring watchdog re-fires on PR #33 and PR #36). Resolution: STORY-005 Option A doctrine (5 soul doc amendments) per owner override on Issue #10. Option C (verdict:* sentinel label) implemented in Sprint 2 P1 per [Issue #38](https://github.com/atilcan65/AtilCalculator/issues/38) (PR #36 design MERGED ecaba25). | Sprint 0 close-out (PR #5 stale-cc at 14:21:30Z, 2026-06-17) | **H** (escalated M → H after 3rd instance) | Sprint 2 P1 merge of Option C (Issue #38) | @orchestrator (doctrine), @architect (design PR #36, MERGED) |
| TD-002 | Review-process gap: architect 🟢 OK without execution evidence can hide CI no-op bugs. PR #23 + PR #26 review chain relied on vacuous CI green (Issue #27). Resolution: R-3 (ADR-0019) §Observability codifies d007-api-observability.sh regression test (Ships in PR #37). | Issue #27 escalation at 17:23:21Z, 2026-06-17 | **H** (already corrupted Sprint 1 review chain) | d007 merged (PR #37 + paired implementation PR) | @architect (design), @tester (PR #37 author) |
| TD-003 | Multi-agent branch-contention: concurrent agent activity on shared filesystem can switch working branch under the active agent's feet. Encountered 2026-06-17 18:31Z when @architect's commit landed on @product-manager's `feat/sprint-1-proposed-scope` instead of intended `docs/adr-0019-api-contract`. Recovery: reset peer's branch + restore file + force-push to correct branch. Resolution options: (a) explicit `git rev-parse --abbrev-ref HEAD` before every commit, (b) per-agent working copies (filesystem isolation), (c) pre-commit hook asserting branch ownership. **Resolution: option (a) worktrees shipped 2026-06-18 — see TD-009 / [td-009-resolution.md](sprints/sprint-01/td-009-resolution.md).** | PR #33 commit recovery, 2026-06-17 18:31Z | **M** (recoverable; no data loss; mild trust cost) | ~~Pick one resolution option in Sprint 1 retro~~ — RESOLVED 2026-06-18 (see TD-009) | @architect (proposes), @atilcan65 (decides — touches workflow / filesystem) |
| TD-004 | **`gh pr edit` silent label-flip failure** (NEW). Calling `gh pr edit N --add-label X --remove-label Y --remove-label Z` in a single invocation may fail silently — the command outputs only the PR URL, no success/error distinction, no exit-code check recommended. First observed 2026-06-17 19:55Z on PR #37 (label flip appeared to succeed but labels were not actually changed; only caught 12 min later via stale_cc-37 watchdog). Resolution: (a) split label operations into separate `gh pr edit` invocations, AND (b) **always re-query the label state via `gh pr view N --json labels` after every flip** (never trust the command output alone). Owner process note: every label flip MUST be followed by a verification query. | PR #37 label flip retry, 2026-06-17 19:55Z (caught 20:07Z via watchdog) | **H** (silent failure; undetectable without external watchdog; can stall PR cycles for minutes) | Owner signs off on process note; architect adds verification step to all label flips | @architect (filed), @atilcan65 (sign-off) |
| TD-005 | **Stale label state after multi-agent checkout** (ROOT CAUSE CONFIRMED). Observed 2026-06-17 20:30Z on PR #37: after architect's label flip retry "succeeded" (verified at 20:14Z), architect's `cc:architect` + `needs-architect-review` were reverted by an external process by 20:30Z. **Root cause confirmed 2026-06-17 20:50:00Z** via @orchestrator's PR #37 comment: orchestrator's PR #32 merge hygiene at 20:23:57Z bulk-removed `cc:orchestrator` and accidentally hit architect's labels in the same transaction. **Doctrine gap**: bulk label hygiene operations are non-selective. Filed follow-up as **TD-006**. | PR #37 label flip verification, 2026-06-17 20:30Z (root cause confirmed 20:50:00Z) | **M** (recoverable; detected by stale_cc watchdog; no data loss) | TD-006 fix in Sprint 2 | @architect (filed), @orchestrator (root cause confirmed) |
| TD-006 | **Multi-role bulk label hygiene hits unrelated roles** (NEW). Observed 2026-06-17 20:23:57Z on PR #37: @orchestrator's PR #32 merge hygiene used a `gh pr edit --remove-label cc:orchestrator` pattern that non-selectively removed other actors' `cc:*` labels in the same transaction. Specifically hit architect's `cc:architect` + `needs-architect-review` that were set 8 minutes earlier (20:14Z). **Doctrine gap**: no transactional boundary around label flips during merge hygiene. **Resolution path** (status as of 2026-06-19, [Issue #46](https://github.com/atilcan65/AtilCalculator/issues/46) P0 chore): the three original resolution options map to accepted/proposed ADRs — (a) orchestrator's hygiene script read-then-remove → **out of scope of this TD** (orchestrator's lane, separate from architect's design work); (b) architect's flips re-verified by watchdog → **structurally addressed by ADR-0024** (watchdog now targets `verdict-by:<ts>` deadlines, not label presence, so architect's flips are checked against the verdict expectation, not the wall clock); (c) label-check workflow snapshot + diff + selective revert → **structurally addressed by ADR-0020** (atomic wrapper + CI gate enforcing read-then-apply-then-verify-then-rollback). The **docs-PR subclass** spam (the `stale_cc` watchdog firing every 15 min on `type:docs` PRs with reflexive peer `cc:*`) is **addressed by ADR-0021** (doctrine convention: `type:docs` PRs default to `agent:<author>` only, no peer `cc:*` without explicit `## Peer review rationale` section). **Validation** (Issue #46 AC: 1 sprint without TD-006-class spam) runs 2026-06-19 → 2026-07-02 in parallel with the ADR-0024 back-compat shim. **2nd instance (PR #110, 2026-06-19)**: `cc:orchestrator` was silently removed **twice within 28 minutes** (04:57Z and 05:25Z) on PR #110 despite re-adds by @architect each time. Mechanism: unknown — possibly orchestrator's automation runs every ~30 min and non-selectively hits `cc:*` labels (same TD-006 mechanism as the original PR #37 instance). Different from original: PR #110 is a docs PR, not a chore PR; orchestrator's intent (per their 05:00Z comment) was to drop `cc:orchestrator` but the actual label state showed it still present at 05:25Z — suggesting orchestrator's automation either tried but failed (TD-004 silent-failure), or fired on a stale PR state, or is unrelated. **Mitigation applied**: @architect re-added `cc:orchestrator` both times within minutes of detection (TD-004/TD-008 protocol); flagged in PR #110 comment 05:30Z as audit trail; not blocking merge. **Action**: monitor for 3rd instance; if observed, escalate to @atilcan65 per stuck-loop doctrine. **Validation impact**: this 2nd instance is WITHIN the Issue #46 validation window (2026-06-19 → 2026-07-02); the doctrine fixes (ADR-0020/0021/0024/0025) have NOT yet been validated in production. | PR #37 reverted at 20:23:57Z, detected 20:30Z, root cause confirmed 20:50:00Z. PR #110 instances: 04:57Z + 05:25Z (2026-06-19), both re-added by architect within minutes. | **M** (caught by TD-005 watchdog within 7 min on PR #37; manual re-add on PR #110; no merge damage; trust cost to orchestrator + architect workflow tax) | Resolution: ADR-0020 (option c) + ADR-0021 (docs subclass) + ADR-0024 (watchdog schema) accepted/proposed; ADR-0025 (bound standby + queue-empty wake) proposed in PR #110 — together close the TD-006 family. Orchestrator's lane (option a) still TODO. 1-sprint validation window per Issue #46: 2026-06-19 → 2026-07-02. | @architect (design + ADR-0024 + ADR-0025), @orchestrator (option a hygiene script), @developer (ADR-0024 + ADR-0025 implementation), @tester (regression tests d012/d013), @atilcan65 (approve ADR-0024/0025 doctrine changes) |
| TD-007 | **Architect posted review BEFORE reading peer's blockers** (NEW). Process gap: on 2026-06-17 19:50:06Z, @developer raised 2 blockers (d007 placement, Issue #35 ownership) on PR #37. At 19:51:06Z, @architect posted 🟢 APPROVE on the design-alignment lens — WITHOUT reading the developer's comment first. The verdict was correct on the architectural lens; the workflow was wrong (the developer's blockers were the architect's decision items, not just dev opinions). Caught by user feedback 20:30Z ("ya developer fikrini sordu okumadın mı?"). **Doctrine gap**: architect's review checklist must explicitly include "read all peer comments raised in the last N minutes before posting verdict" — not just the diff against the design doc. | PR #37 architect review 19:51:06Z, caught 20:30Z via user feedback | **M** (correct verdict, wrong workflow; no merge damage; user-visible trust cost) | Architect adds "read peer comments within last 30 min" to standard review checklist; owner approves checklist update | @architect (filed; checklist owner), @atilcan65 (approve) |
| TD-008 | **`gh pr edit` removes MORE labels than requested** (NEW; same family as TD-004). On 2026-06-17 21:25:00Z, @architect ran two SEPARATE `gh pr edit 42 --remove-label X` calls (X = `needs-architect-review`, then `cc:architect`). Verification via `gh pr view 42 --json labels` showed **4 labels removed** (the 2 requested + `cc:tester` + `needs-tester-signoff`) and 1 added (`cc:developer`, not in original state). Both `gh pr edit` invocations returned the PR URL with no error. **Root cause unclear**: could be (a) `gh pr edit` does not isolate label changes per invocation, (b) another agent's automation ran between the two calls, (c) label-check workflow auto-cleaned unreferenced reviewer labels. Mitigation: (1) `gh pr view --json labels` after EVERY individual `--remove-label` call (not just after the batch), (2) re-add missing labels immediately on detection, (3) flag the re-occurrence in PR review for human awareness. **Data point for TD-004 family**: silent label-flip failures are not just "no change" but also "more change than requested". | PR #42 label flip 21:25:00Z, verified 21:25:30Z | **H** (silent over-removal; if not caught, PR cycle stalls or wrong reviewer wakes) | Owner + GH support investigate `gh pr edit` semantics; architect documents the "remove N, verify, remove N+1" pattern in checklist | @architect (filed), @atilcan65 (sign-off), GH support (TBD) |
| TD-010 | **Tester soul §Handoff Discipline missing `--remove-label status:in-review` in APPROVED path** (NEW). Symptom: PR #62 had **68 phantom wakes** from `stale_cc` watchdog re-firing on the same event (2026-06-17, post-architect+tester+orchestrator 🟢). Root cause: tester wrote APPROVED comment but did not flip `status:in-review` → `status:ready`; queue still said "in review"; watcher's `processed_event_ids` dedup stopped re-processing the same event ID but every new poll cycle re-read the dirty label state. Orchestrator's atomic flip unblocked PR #62. **Doctrine gap**: "approved in comment" ≠ "approved in queue"; soul §Handoff Discipline must include BOTH the comment AND the label flip. Fix: tester soul §Handoff Discipline amendment: APPROVED path includes `gh pr edit N --remove-label status:in-review --remove-label needs-tester-signoff --add-label status:ready` as a single atomic transition. Filed per Sprint 1 retro A4 (2026-06-18). | Sprint 1 retro A4, 2026-06-18 | **M** (recoverable; no merge damage; phantom wake noise = agent throughput tax) | Tester soul amendment merged in Sprint 2 P1 (PR sized separately; ~0.5 SP) | @architect (filed), @tester (soul amendment owner) |
| TD-011 | **PM `agent-watch.sh` lacks issue-level events** (NEW). PM is supposed to wake on `issue_assigned` to stories they should groom, but `scripts/agent-watch.sh product-manager` does not currently emit `issue_assigned` events for PM-targeted issues (the script is `pr_review_requested` + `pr_comment_mention` + `pr_new_commit` only). PM worked around in Sprint 1 via (a) manual board checks on the Project v2 backlog, (b) `notify.sh` pings from @orchestrator. **Doctrine gap**: ADR-0002 (Autonomy Loop) defines `issue_assigned` as the wake trigger for story grooming, but the watcher script doesn't include it for PM. Fix options: (a) extend `scripts/agent-watch.sh` to include `issue_assigned` for PM, (b) PM soul adds manual board scan to daily hygiene, (c) hybrid (watcher extension + PM soul reminder). Owner decision needed on (a/b/c). Filed per Sprint 1 retro A5 (2026-06-18). | Sprint 1 retro A5, 2026-06-18 | **L** (workaround exists; not blocking Sprint 2; quality-of-life debt) | Owner signs off on (a) or (b) or (c); implementation in Sprint 2 P1 (≤1 SP) | @architect (filed), @atilcan65 (decide path), @product-manager (consumer) |
| TD-012 | **4-cat invariant vs. Handoff Discipline conflict in `status:ready` (human-merge) state** (NEW). Observed 2026-06-19 on PR #103: architect removed `cc:architect` per architect.md §Handoff Discipline (after posting 🟢 OK COMMENTED review on a `status:ready` PR); the removal left **zero `cc:*` labels** on the PR, breaking ADR-0012 4-cat invariant (`type:*` + `status:*` + `agent:*` + `cc:*` required). Architect re-added `cc:architect` to restore 4-cat (label-check CI would otherwise fail). **Doctrine gap**: (1) architect.md §Handoff Discipline says to remove `cc:architect` after review is done; (2) ADR-0012 4-cat invariant requires at least one `cc:*`. These two are in conflict when the acting agent is the **sole** `cc:*` holder AND the PR has moved past the reviewer chain (i.e., into `status:ready` for human merge). Available `cc:*` labels are the 5 standard roles — `cc:human` does not exist (per `cc:human` not in label taxonomy). The label-check workflow (`.github/workflows/label-check.yml`) enforces the 4-cat invariant and will fail on `status:ready` PRs that have no `cc:*`. Fix options: (a) doctrine amendment: `cc:architect` (and analogous sole-`cc:*` labels) **stays on** `status:ready` PRs as the design-OK gate; architect's queue cleanup is then "remove `needs-architect-review` only, keep `cc:architect` until merge"; (b) system: introduce `cc:human` label and auto-add in `status:ready` state (requires label-creation PR + label-check workflow update); (c) system: 4-cat `cc:*` requirement relaxed in `status:ready` state via label-check workflow exception. **Workaround** (current, ad-hoc): re-add `cc:architect` immediately after the violating removal; this defeats the queue cleanup intent and produces the TD-006-class watcher re-fires (architect stays in `cc:*` queue → watcher wakes on every label change). **Validation**: watch the next `status:ready` PR cycle for the same pattern. Filed per architect observation during PR #103 review. | PR #103 cc:architect flip cycle, 2026-06-19T00:28Z | **M** (workaround exists; CI label-check catches the gap; recoverable; agent throughput tax from re-fires) | Owner + architect decide (a) / (b) / (c) in Sprint 2 retro; implementation in Sprint 2 P1 (~0.5 SP for option a, ~1-2 SP for b/c) | @architect (filed), @atilcan65 (decide path), @orchestrator (implementation if b/c) |
| TD-013 | **HWM-advancing re-fire pattern on stable PRs with persistent `cc:*`** (NEW). Canonical instance: PR #120 on 2026-06-19 (after architect posted 🟢 APPROVE design-alignment at 09:24Z, awaiting BDFL merge at 09:50Z). The `scripts/agent-watch.sh` watcher emitted **5+ `pr_review_requested` wakes within ~7 minutes** (09:25Z → 09:31Z) on the **same SHA `2b8bb0f` with identical labels** (`type:chore, status:ready, agent:developer, cc:architect, verdict-by:2026-06-19T09:50:00Z`). **Root cause hypothesis**: the watcher's `processed_event_ids` dedup table suppresses repeat events of the **same kind+id**, but the GitHub `pr_review_requested` / `pr_labeled` / `pr_new_commit` event IDs are **deterministic per-state-change** — when state doesn't change, the events are emitted with the same ID and SHOULD be deduped. However, the wake payload also includes the `pr_review_requested` shape (labels + branch + head_sha) which may be re-emitted when the watcher polls a slightly different API endpoint, generating new event ids that bypass dedup. **Observed also on**: PR #110 (`cc:orchestrator` silent removal/re-add cycle, 28 min, 2 instances — TD-006 family); PR #93 (stale_cc own-PR self-cc — Issue #94 root cause). **Mitigation in place**: per TD-012, `cc:architect` stays on `status:ready` PRs (cannot be removed without 4-cat violation); the re-fires are **noise-only**, no action required per ADR-0025 §1 bounded standby. **Cost**: agent throughput tax (~10-20% of wake cycles on stable PRs are redundant); not blocking; not data-corrupting. **Fix options**: (a) extend `processed_event_ids` dedup to include a **state-stability hash** (e.g., SHA + label-hash), suppress wake if (sha, label-hash) unchanged from last processed; (b) introduce **re-fire TTL** on `pr_review_requested` / `pr_labeled` events (e.g., suppress if same event id seen within 5 min — same scheme as ADR-0024 verdict-by:<ts> throttle); (c) document the pattern as expected behavior and tolerate re-fires (current state). **Recommendation**: option (b) — minimal code change, consistent with existing throttle scheme, bounded cost. **Implementation**: `scripts/agent-watch.sh` poll_once dedup gate (~10 lines + 1 regression test in d016-stability-re-fire-prevention.sh). Filed after observing 5+ instances on PR #120 within a single bounded-standby session. | PR #120 re-fire cluster 2026-06-19T09:25Z → 09:31Z (5+ wakes, same SHA + labels) | **L** (noise-only; bounded standby + TD-012 keep-cost; not data-corrupting; throughput tax only) | Owner + architect decide (a) / (b) / (c) in Sprint 2 retro; option (b) implementable in Sprint 2 P1 (~0.5 SP); option (a) more invasive (~1-2 SP) | @architect (filed), @developer (impl if b selected), @tester (regression test d016), @atilcan65 (decide path) |
| TD-015 | **ADR-0027 §Decision.1 architectural mistake** (NEW). Chose public GH Actions runner for the deploy pipeline, but GH public runners **cannot reach private LAN IPs** (192.168.1.199:22). First auto-deploy attempt at 2026-06-19T20:12:47Z (PR #137 merge → workflow run #27846373740) failed at SSH dial — fundamental design flaw, not config. **Root cause**: ADR-0027 §Alternatives rejected Option D (self-hosted runner) for "runner maintenance burden" — that cost is small vs. the LAN reachability problem (which is intrinsic to public runners). **Fix**: ADR-0030 (proposed in this PR) supersedes ADR-0027 §Decision.1 with self-hosted GH runner. Sprint 3 P0 unblocked once ADR-0030 Accepted + owner installs runner + workflow YAML updated. **Severity**: **M** (corrected within Sprint 3, not a recurring pattern; P0 incident contained; prod was NOT modified — failure at SSH dial before any prod change). **Payoff trigger**: Sprint 3 retro (capture "design alternatives must be evaluated for fundamental reachability" lesson for future ADRs). **Architectural lesson for future ADRs**: when designing CI/CD paths, **always test the network reachability of the runner to the target host** as a gating criterion BEFORE choosing the runner type. Self-hosted is the boring default for LAN targets; public runner is for SaaS deploys only. | Issue #138 (P0 incident, opened 2026-06-19T20:13:46Z); Issue #139 (architect decision request); PR #136 (DEPLOY-001 impl, merged 2026-06-19T19:24Z); PR #137 (Sprint 3 plan, merged 2026-06-19T20:12Z — triggered the failed deploy) | **M** (P0 contained; corrected within Sprint 3 via ADR-0030; not a recurring pattern) | Sprint 3 retro (2026-06-20T18:34Z start per RETRO-003 schedule); document "network-reachability-as-gating-criterion" lesson in retro + future ADR template | @architect (filed + ADR-0030 author), @developer (RCA-3 fix + workflow YAML update), @human (owner gate on runner install + workflow file merge), @atilcan65 (approve ADR-0030 doctrine change) |
| TD-016 | **PR #146 architect review missed end-to-end data flow** (NEW). On PR #146 (workflow YAML update, merged 40242a2d at 2026-06-19T21:34:24Z), @architect posted 🟢 APPROVE design-alignment without tracing 2 end-to-end data flows: (1) **REPO_DIR hardcoded path**: `scripts/deploy-runner.sh` line 32 default `REPO_DIR="${REPO_DIR:-$HOME/projects/AtilCalculator}"` — wrong for self-hosted runner which checks out to `$GITHUB_WORKSPACE=/home/gh-actions-runner/actions-runner/_work/AtilCalculator/AtilCalculator/`. Result: first self-hosted deploy failed at REPO_DIR check (Issue #148 RCA-5). (2) **Notify step missing TELEGRAM env refs**: `Notify owner (always)` step env block has `GITHUB_SHA` only — no `TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}` or `TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}` reference. Result: notification silently fails even on successful deploy (Issue #148 RCA-6). **Root cause**: architect review checked local YAML structure but did not trace data flow end-to-end (where does REPO_DIR come from in script? where do TELEGRAM vars get bound for `bash scripts/notify.sh`?). **Same class as TD-015** (architectural-blind-spot on end-to-end contract, not local shape). **Fix**: 2-line fix-PR by @developer (REPO_DIR default → `$GITHUB_WORKSPACE`; workflow YAML job-level env adds TELEGRAM refs); architect's next review explicitly checks end-to-end data flow before verdict. **Severity**: **M** (corrected within Sprint 3, P0 contained; prod NOT modified — script bailed at REPO_DIR check before any prod mutation; same severity as TD-015 because same-class lesson). **Payoff trigger**: Sprint 3 retro (capture "end-to-end-data-flow-as-gating-criterion" lesson for future architect reviews). **Architectural lesson for future reviews**: when reviewing a PR that crosses component boundaries (YAML ↔ script ↔ runner ↔ prod), trace each variable/secret end-to-end from source (where it's defined) to sink (where it's consumed). Local-shape correctness is necessary but not sufficient. | PR #146 (merged 40242a2d 2026-06-19T21:34:24Z); PR #146 review by @architect at 21:18:34Z (verdict 🟢 before first deploy 21:34:33Z); Issue #148 (P0, opened 21:35:11Z); workflow run #27849461286 (FAILED 21:34:33Z, 7s elapsed) | **M** (same class as TD-015; P0 contained; corrected within Sprint 3 via developer follow-up PR; not a recurring pattern yet) | Sprint 3 retro (2026-06-20T18:34Z); document "end-to-end-data-flow-trace-as-gating-criterion" in retro + add to architect review checklist + consider ADR-0025 amendment for next-sprint review process | @architect (filed + owns the miss), @developer (2-line fix-PR on branch fix/deploy-001-rca-5-6), @tester (sign-off on deploy-runner.sh contract preservation + workflow YAML env bindings), @human (owner gate on workflow file merge) |
| TD-018 | **PR #151 architect review missed RUNTIME PRECONDITIONS** (NEW, parallel to TD-016 but for runtime scope). On PR #151 (RCA-5+RCA-6 fix, squash-merged e13407d at 2026-06-20T04:05:26Z), @architect posted 🟢 APPROVE design-alignment with 12/12 architecture rows PASS + 4/4 data flow links verified for both fixes. After merge, **2 self-hosted deploys FAILED** with new RCAs (Issue #152 + Issue #153): (1) **RCA-7** — `systemctl --user restart atilcalc-web.service` cannot reach D-Bus session bus from self-hosted runner's runtime context. The runner process (`gh-actions-runner`) has no active systemd user session unless `loginctl enable-linger gh-actions-runner` is set on prod. My PR #151 review traced the restart command syntax but did NOT check whether the OS-level systemd user bus was reachable from the runner's non-login runtime context. (2) **RCA-8** — `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` repo secrets have **empty values** (most likely: `~/.dev-studio-env` had literal empty `TELEGRAM_BOT_TOKEN=` line piped to `gh secret set` per Issue #143 AC #7). My PR #151 review traced `secrets.X` → step env → script env read SYNTACTICALLY, but did NOT verify the secrets have non-empty VALUES at runtime. (3) **Auto-rollback gap** — `set -euo pipefail` causes script abort at step 2 on any failure; rollback + page steps are SKIPPED. My review did not flag the missing defensive wrap (`|| { rollback; page; exit 2; }`). **Root cause**: architect review traced DATA FLOW (static shape) but missed RUNTIME PRECONDITIONS (OS-level service availability, secret value non-emptiness, error-recovery wrapping). **Same class as TD-016** (end-to-end contract blind spot, not local shape), but different scope: TD-016 = static data flow, TD-018 = runtime preconditions. **Severity**: **M** (P0 contained within Sprint 3; fix-PR in flight; same severity as TD-015/TD-016). **Payoff trigger**: Sprint 3 retro (capture "runtime-preconditions-as-gating-criterion" lesson). **Architectural lesson for future reviews**: when reviewing a PR that crosses component boundaries (YAML ↔ script ↔ runner ↔ prod host), trace not just data flow but runtime preconditions — (a) verify referenced values have non-empty/valid contents, not just non-null references; (b) verify OS-level service prerequisites (systemd bus, dbus, linger) are met for the calling context; (c) verify error-recovery paths (rollback, owner-page) are NOT silently skipped by early-exit semantics. | PR #151 (squash-merged e13407d 2026-06-20T04:05:26Z); PR #151 review by @architect at 21:55Z (verdict 🟢 before first deploy 04:05:35Z); Issue #152 (P0, opened 04:06Z, first post-#151 failure); Issue #153 (P0, opened 04:10Z, second post-#151 failure); workflow run #27859671427 (FAILED 04:05:35Z, 7s elapsed); Issue #148 (P0 RCA-5/6 — TD-016 predecessor) | **M** (same class as TD-015/TD-016; P0 contained; corrected within Sprint 3 via owner gates + dev fix-PR in flight; 2nd instance of end-to-end blind spot — pattern emerging) | Sprint 3 retro (2026-06-20T18:34Z); document "runtime-preconditions-as-gating-criterion" lesson in retro + extend architect review checklist to include (a) value non-emptiness, (b) OS-level service prerequisites, (c) error-recovery wrap verification; consider ADR-0025 amendment | @architect (filed + owns the miss), @developer (defensive script wrap + test plan additions TC-11/12/13, AP-11), @human (owner gates: loginctl enable-linger + TELEGRAM secret value fix), @tester (regression test for runtime preconditions) |
| TD-019 | **Orchestrator guidance cross-check doctrine** (NEW, sister to TD-016 + TD-018 but on the **orchestrator** side, not architect). On Issue #152 (P0, RCA-7 deploy failure), @orchestrator posted guidance at 2026-06-20T04:47Z that included a **hallucinated module path** `atilcalc.web.app:app`. Canonical module is `atilcalc.api.main:app` (confirmed by 12 references in repo: `scripts/run-server.sh` + 11 test files). `atilcalc.web` package exists but contains only an empty `__init__.py` (vanilla JS Web Components, no Python `app` object). Orchestrator's 04:47Z guidance would have caused script failure at import time if followed verbatim. **Root cause**: @orchestrator did not cross-check the canonical entry script (`scripts/run-server.sh`) before issuing prod-host commands. The guidance was issued under P0 time pressure (deploy was failing, owner was paged), and the wrong path slipped through. **Same family as TD-016 + TD-018** (end-to-end contract blind spot, not local shape), but different scope: TD-016 = architect missed data flow on PR review; TD-018 = architect missed runtime preconditions on PR review; **TD-019 = orchestrator missed canonical-entry verification on guidance issuance**. Together TD-016/TD-018/TD-019 form a "blind-spot family" — all three are instances of "agent must trace MORE than local shape before issuing verdicts/guidance". **Fix**: doctrine amendment (in this PR's CHANGELOG + ADR-0027 supplement) — before issuing prod-host commands, workflow YAML snippets, or design doc recommendations, the agent MUST grep the canonical entry script (or equivalent) and confirm: (a) module path, (b) restart mechanism, (c) preflight steps, (d) post-deploy verification command. The `scripts/run-server.sh` is the canonical for AtilCalculator; other repos may have a `Makefile`, `pyproject.toml` `[project.scripts]`, or similar. **Sister entry consolidation**: see "Blind-spot family" section below. **Severity**: **M** (P0 contained via 04:39Z owner diagnose + 05:02:42Z manual unblock + 05:03:51Z orchestrator RCA — the wrong module path was caught by owner during manual unblock before any scripted deployment used it; no prod damage from the wrong path itself). **Payoff trigger**: Sprint 3 retro (RETRO-003, 2026-06-20T18:34Z); document "canonical-entry-cross-check doctrine" + consolidate TD-016/018/019 into a single "blind-spot family" entry per the RETRO-003 plan. | Issue #152 cmt 4756498070 (orchestrator's 04:47Z guidance with hallucinated `atilcalc.web.app:app`); Issue #152 cmt 4756543400 (orchestrator's 05:03:51Z RCA — caught the error via scripts/run-server.sh cross-check); Issue #156 (docs task for TD-019) | **M** (P0 contained; no prod damage; caught at owner manual unblock; same severity as TD-015/016/018 — same class of lesson, different scope) | Sprint 3 retro (RETRO-003); consolidate TD-016 + TD-018 + TD-019 into single "blind-spot family" entry; document cross-check doctrine in architect + orchestrator soul files (`.claude/agents/architect.md`, `.claude/agents/orchestrator.md`); optional CI lint check (`scripts/tests/d019-canonical-entry-cross-check.sh`) grepping `deploy-runner.sh` against `scripts/run-server.sh` for module-path consistency | @orchestrator (filed + owns the miss; filed Issue #156 + cross-check doctrine), @architect (this docs PR + ADR-0027 supplement + RETRO-003 consolidation), @developer (optional CI lint check d019), @tester (regression test d019 if implemented), @atilcan65 (approve soul-file doctrine amendment) |
| TD-020 | **PR #157 architect review missed SILENT-SKIP PREFLIGHT (WARN-only when .venv missing)** (NEW, sister to TD-018 but on a different layer). On PR #157 (DEPLOY-001 v5 nohup+setsid rewrite, merged c7c060e at 2026-06-20T06:04:44Z), @architect posted 🟢 APPROVE design-alignment with 6-lens verdict (4 GREEN, 2 YELLOW advisory). My L2 (Runtime preconditions) lens said "🟢 GREEN — all 4 layers addressed: (a) venv deps install preflight (RCA-7-4 fix)". First self-hosted auto-deploy at 06:04:46Z (run #27862367000) **FAILED** with **RCA-9** (Issue #160): `.venv` directory does NOT exist on the self-hosted runner's `$GITHUB_WORKSPACE` after `git reset --hard origin/main`. My preflight dep install was **WARN-only** when `.venv` missing: log line `WARN: .venv or uv not found at ... — skipping preflight dep install (RCA-7-4 risk)`. The script then proceeded to step 4 (nohup+setsid restart) which references `$REPO_DIR/.venv/bin/uvicorn` — file doesn't exist, restart failed. **Root cause**: I approved a preflight that **silently skipped** when its precondition (`.venv` existence) was not met. The semantic intent was "ensure deps are installed" (FAIL-or-CREATE), but the code was "if `.venv` exists, install deps; else warn and continue" — the WARN-only branch converts a hard failure into a soft failure, breaking the preflight's contract. **Same family as TD-016 + TD-018 + TD-019** (end-to-end contract blind spot — agent verified local shape but missed semantic intent enforcement), but different layer: TD-016 = static data flow, TD-018 = runtime preconditions (D-Bus, secrets), TD-019 = canonical-entry, **TD-020 = silent-skip preflight pattern (WARN-only when precondition missing)**. The tester's parallel miss (AP-14 said "presence of preflight is mandatory" but didn't cover "WARN-only skip" path) compounds the lesson — both review lenses missed that "presence" is not "enforcement". **Fix**: (a) deploy-runner.sh v6 changes preflight dep install from `if [.venv exists] install deps; else WARN-skip` to `if [.venv missing] uv venv; then uv pip install (FAIL on non-zero exit); else fail` — FAIL-or-CREATE pattern, NOT WARN-only. (b) PR #150 v7 test plan amendment adds TC-15 NEW (preflight dep install CREATES .venv if missing), AP-21 NEW (probe: script fails fast on .venv creation failure, no WARN-only skip path), AP-22 NEW (probe: `uv pip install` non-zero exit is non-zero script exit, NOT log-only). (c) Architect's 6-lens checklist adds **(g) silent-skip preflight pattern**: any preflight step that WARN-only-skips on missing preconditions is a structural risk — review must verify the FAIL/CONTINUE branch is intentional, not a silent-failure-by-default. **Severity**: **M** (P0 contained; first deploy after merge failed; no prod damage — `git reset --hard` reverted source but no service started; same severity as TD-015/016/018/019). **Payoff trigger**: Sprint 3 retro (RETRO-003, 2026-06-20T18:34Z); consolidate TD-016/TD-018/TD-019/TD-020 into single "blind-spot family" entry with **7-lens architect review checklist** (a-g); update 6-lens to 7-lens in `.claude/agents/architect.md` and `docs/decisions/ADR-0025-autonomy-loop.md` if it references the checklist. | PR #157 (squash-merged c7c060e at 2026-06-20T06:04:44Z); PR #157 review by @architect at 05:24:37Z (v1) + 05:44Z (re-confirmation on aa6b91d) — both 🟢 before first deploy 06:04:46Z; Issue #160 (P0, opened 06:08Z by @tester, RCA-9 silent-skip preflight); workflow run #27862367000 (FAILED 06:04:46Z, 2s elapsed); Issue #155 (DEPLOY-001 v5 parent story); Issue #152 (P0 RCA-7 — TD-018/019 predecessor); PR #151 (TD-018 victim, RCA-7 culprit); PR #150 (test plan, tester filed AP-14 miss as TD-022 candidate) | **M** (P0 contained within Sprint 3; fix-PR in flight; same severity as TD-015/016/018/019 — same class of lesson, different scope; 4th instance of end-to-end blind spot — pattern CONFIRMED, no longer "emerging") | Sprint 3 retro (RETRO-003); consolidate TD-016 + TD-018 + TD-019 + TD-020 into single "blind-spot family" entry; document **7-lens architect review checklist** (a-g); update architect soul file (`.claude/agents/architect.md`) with the new (g) silent-skip preflight pattern check; update tester AP canon (PR #150 v7) with TC-15 NEW + AP-21/22 NEW; new PR for deploy-runner.sh v6 (FAIL-or-CREATE pattern) | @architect (filed + owns the miss), @developer (deploy-runner.sh v6 FAIL-or-CREATE fix + PR #150 v7 test plan amendment), @tester (file TD-022 tester-side parallel miss; PR #150 v7 TC-15/AP-21/22), @orchestrator (Issue #160 triage + Issue #125 family priority bump + REPRIME spot-check cadence review), @human (owner gate on workflow file merge + Issue #125 fix; new PR #157 follow-up squash-merge) |
| TD-023 | **Multi-repo watcher coverage gap — Option A deferred** (NEW, Issue #265 P0 BLOCKER). On 2026-06-22T15:54Z, owner opened 3 PRs in `atilcan65/dev-studio-template` (PR #42 atomic-write+d027, PR #43 label-taxonomy+bootstrap, PR #44 Auto-Ping dual-channel). Arch + test watcher logs show **zero events** for template PRs (15:44→16:03Z, 7 empty polls); both agents' `scripts/agent-watch.sh` is **single-REPO per invocation**, bound to `atilcan65/AtilCalculator` only via systemd env files. Result: 5 GAP KAPATMA issues (#259-#263) opened in AtilCalc tracker have code-PRs in template repo; arch + test can't review them via watcher. Worked around with direct GitHub mentions on PR #42 + #43 + #44 (Telegram ping alone is insufficient — arch/test action loop is watcher-driven). **Root cause**: `scripts/agent-watch.sh` `REPO` env var is single-value; no multi-repo loop. **Fix options** (Issue #265): (A) extend `REPO` to comma-separated list + per-repo state dedup + new test `d024-multi-repo-watcher.sh` (~0.5 SP, touches agent-watch.sh — CRITICAL INFRA per recent session feedback); (B) migrate 5 GAP KAPATMA issues from AtilCalc → template repo (~0.2 SP, no infra change, fully reversible); (C) mirror PRs in AtilCalc (~0.3 SP × N, high friction); (D) owner-only review path (bypasses ADR-0009 §10.5.4). **Architect verdict** (Issue #265 cmt 4771760979, 2026-06-22T18:48Z): **Option B for now** (execute this session, ~30 min, unblocks Sprint 4 EOD). **Option A deferred** — agent-watch.sh sacred (close-call recovery on PR #249 rebase per owner feedback "üstad bozuyorsun surekli agent watch'ı" / "geri almamız gerek bu kod olmaz"); Option A's value (1 template PR/day max) does not justify touching critical infra. YAGNI applies. **Payoff trigger**: when `atilcan65/dev-studio-template` PR rate exceeds **2 PRs/week for 2 consecutive sprints** → re-open as P1, schedule Option A in next sprint (~0.5 SP, owner gate on agent-watch.sh change). If rate stays < 1/week for 2 sprints → close as YAGNI-confirmed. **Severity**: **M** (P0 unblocked via Option B workaround; no merge damage; reversible; throughput tax ~10 min/PR for arch+test direct review). **Architectural lesson**: when agent infra is single-tenant, multi-tenant demand forces either (a) generalize infra or (b) migrate work to where infra works. (b) wins when (a)'s cost > (b)'s one-time + future migration cost. Reversibility > correctness. | Issue #265 (P0 BLOCKER, opened 2026-06-22T16:04Z); Issue #265 cmt 4771760979 (architect verdict 2026-06-22T18:48Z, Option B recommended); PR #42 (atomic-write+d027), PR #43 (label-taxonomy), PR #44 (dual-channel) in atilcan65/dev-studio-template; AtilCalc issues #259-#263 (GAP KAPATMA tracker) | **M** (P0 unblocked via Option B workaround; no merge damage; reversible — closes if Option A never triggers within 4-sprint window; throughput tax only) | Sprint 5+ eval: track template-repo PR rate (GH API query on `atilcan65/dev-studio-template` weekly). When rate > 2/week for 2 consecutive sprints → re-open as P1, schedule Option A (~0.5 SP, owner gate on agent-watch.sh). If rate < 1/week for 2 sprints → close as YAGNI-confirmed. | @architect (filed + architect verdict on #265), @developer (Option B migration execution + future Option A impl), @tester (d024 regression if Option A executes), @atilcan65 (Option B immediate decision + Option A future gate on agent-watch.sh) |
| TD-024 | **Soul patch landed via agent-authored PR — deviated from `.claude/` file ownership matrix "human-only territory"** (NEW, PR #275). On 2026-06-22T19:36:27Z, PR #275 (merge commit 608dc45) was squash-merged by @atilcan65 with **4 soul file edits** in `.claude/agents/{architect,developer,product-manager,tester}.md` (+28 lines each, matches the expected `apply-adr-0038-soul-patch.sh` output per orchestrator PR #275 SUMMARIZE comment at 19:35:20Z). PR branch `dev/274-merge-conflict-resolution` was authored by atilcan65 but follows the **agent-PR pattern** (type:bug label, status:done after merge, `dev/274-merge-conflict-resolution` branch name — not `docs/` prefix used for owner-direct doctrine commits). Per `.claude/CLAUDE.md` §File ownership matrix, **`.claude/` is "human-only territory"** — agents may PROPOSE soul file wording via PR comment / design doc, but the COMMITTED change must come from owner direct commit (or owner-gated PR with explicit override). **Doctrinal deviation**: this PR landed soul patches via an agent-style branch. **Architect review was pending** (was about to post 🟢 OK on status flip + 🔴 BLOCK on soul patch per file ownership matrix) but merge happened on orchestrator APPROVE + owner override before architect verdict was posted. **Per ADR-0031 (owner-override doctrine)**, the override is legitimate; per "Things agents must NEVER do" ("never let we'll fix it later leave a meeting without a tech-debt ticket"), the deviation must be documented. **Soul patch CONTENT is correct** (matches ADR-0038 §Layer 1 spec verbatim, including the "After events processed and BEFORE going back to sleep, IF WIP < 2 THEN run: bash scripts/claim-next-ready.sh <role>" trigger wording). **Premature activation observation** (separate concern, not a deviation): soul patch references `scripts/claim-next-ready.sh` which does NOT yet exist on main — developer scope = 1.5 SP, slips Sprint 5 per ADR-0038 §Sprint 4 commitment. Agents running the soul patch today will hit exit 127 (command not found) until dev lands the script. This is anticipated (ADR-0038 explicit) — agents can skip the call on ENOENT and continue their loop. **Fix options** (owner decides): (a) **accept deviation** — amend ADR-0038 §Layer 1 to explicitly enumerate two valid landing paths (owner direct commit OR owner-gated PR with "owner-override" tag in body); the doctrine becomes "owner sole authority on soul files, but landing path is owner's discretion" — update file ownership matrix row `.claude/` to reflect. (b) **reject deviation** — revert soul patch portion of PR #275 (`git revert -n 608dc45` then re-apply just the ADR status flip), re-apply soul patch via owner direct commit on main; preserves the existing doctrine but creates PR #275 follow-up merge tax. **Recommendation**: option (a) — the doctrine intent ("owner sole authority on soul files") is preserved in both paths; the deviation is on the procedural detail (PR vs direct commit) which is reversible and does not change the architectural invariant. The cost of option (b) is higher (extra revert + re-merge cycle + risk of merge-conflict recursion) than the cost of option (a) (one paragraph in ADR-0038 + one row in file ownership matrix table). **Severity**: **M** (doctrinal deviation accepted by owner override; no data corruption; soul patch content correct; setting precedent that agent-authored PRs can edit `.claude/` weakens file ownership matrix invariant if not amended). **Payoff trigger**: when (i) owner wants to revisit file ownership matrix doctrine in Sprint 5 retro, OR (ii) agent-authored PRs editing `.claude/` become routine (>1/week) WITHOUT explicit owner-override tag in PR body. **Architectural lesson**: when owner overrides a file-ownership-matrix boundary on a per-PR basis, the override MUST be explicitly recorded (in PR body, follow-up ADR amendment, or doctrine ledger row) so that the doctrine ledger reflects the deviation. Default-assumed "owner = developer agent" identity conflation is dangerous for boundary doctrine — the architect review checklist should include a "if this PR edits `.claude/` AND was authored on agent-style branch, flag explicit owner-override in PR body or escalate" gate. Filed per architect observation post PR #275 merge. | PR #275 (squash-merged 608dc45 at 2026-06-22T19:36:27Z by @atilcan65); branch `dev/274-merge-conflict-resolution` (agent-PR pattern); 4 files in `.claude/agents/` (+28 each); ADR-0038 §Layer 1 wording verified verbatim in landed patch; orchestrator PR #275 SUMMARIZE comment at 19:35:20Z confirms orchestrator APPROVE on sprint-planning lens but architect review was pending | **M** (doctrinal deviation accepted by owner override; no data corruption; soul patch content correct per ADR-0038; precedent risk if deviation not amended) | Sprint 5 retro; owner decides option (a) vs (b); if (a) — architect authors ADR-0038 §Layer 1 amendment + file ownership matrix row update (~0.25 SP); if (b) — owner reverts soul patch portion + re-applies via direct commit (~0.5 SP merge tax); architect checklist amendment "PR edits `.claude/` on agent-style branch → verify explicit owner-override in body or escalate" is a process gate, low-SP, can ship in Sprint 5 P2 regardless of (a)/(b) choice | @architect (filed + architect checklist amendment author), @atilcan65 (decides option (a) vs (b) + applies doctrine amendment if (a)), @developer (implements checklist amendment in `.claude/agents/architect.md` if owner approves), @orchestrator (Sprint 5 retro input on doctrine ledger convention) |
| TD-025 | **ADR-0041 Phase 0 (PR #322) carries 3 documented limitations to Phase 2** (NEW, Issue #326 P0 sister). Per PR #323 tester verdict OBS-2/OBS-3/OBS-4 (2026-06-23T21:37Z), the standalone script `scripts/agent-watch-verdicts.sh` (Option B) ships intentionally minimal vs the full ADR-0041 §Decision spec: **(1) scope guard partial** — script's `fetch_relevant_prs` queries only `agent:${ROLE}` label, NOT the ADR's broader `cc:<role>` OR `agent:<role>` OR `verdict-by:<ts>`; **(2) no self-cc skip** — script surfaces verdicts on the calling agent's OWN PRs (queue pollution, Issue #94 doctrine violation); **(3) no `context.keyword_matched` emit** — debug field per ADR §Decision event schema is missing. These are **non-blocking** per the verdict (intentional Phase 0 minimal, belt+suspenders cutover per ADR §Consequences) but are real gaps vs the canonical spec. **Fix path**: Issue #326 (filed 2026-06-23T21:37Z, agent:developer, P0) — v8 native extension in `scripts/agent-watch.sh` with the 3 gaps as explicit AC-1/AC-2/AC-3. Sister deliverables: d037 regression (tester, 8 TUs); ADR-0002 §Event Model doctrine amendment (owner-gated, .claude/ file ownership matrix). **Severity**: **M** (P0 RCA closed by Phase 0+1; gaps are for completeness/dx, not function; 5-min poll cycle is rate-limited; 2× Phase 0/1 polling load per ADR §Consequences is acceptable). **Payoff trigger**: Phase 2 native impl ships (Issue #326 AC-1/2/3 GREEN) + d037 GREEN + ADR-0002 amendment merged. | PR #323 verdict OBS-2/3/4 (cmt 4783674168, 2026-06-23T21:37Z); PR #322 (Phase 0 impl); Issue #326 (Phase 2 sister, P0, agent:developer) | **M** (P0 closed; gaps documented as intentional phasing; not data-corrupting; ~10% throughput tax until d037 lands) | Issue #326 (Phase 2 v8 native impl) — Sprint 7 P1 or Sprint 8 P0; d037 sister (tester); ADR-0002 §Event Model amendment (owner-gated) | @architect (filed + PR #323 verdict reviewer), @developer (Issue #326 impl), @tester (d037 8 TUs), @atilcan65 (ADR-0002 amendment owner gate) |
| TD-026 | **PR #333 (PR-B, orchestrator.md + ADR-0042) MERGED with 4 architect findings unaddressed — owner override per ADR-0031** (NEW, Issue #320 sister-ticket, **Findings 2/3/4/5 RESOLVED via PR #334 amendment 2026-06-24T10:47Z**). On 2026-06-24T08:29:13Z, PR #333 (merge commit 955ab57, branch `feat/orchestrator-role-and-adr-41`, +316/-1) was squash-merged by @atilcan65 with **all 4 architect 🟡 SUGGESTION findings unaddressed** (architect verdict cmt 4787180967, 2026-06-24T08:08:16Z). Findings: **(1)** `.claude/agents/orchestrator.md` lines 26, 156, 165 still reference deprecated `scripts/notify.sh -l <role>` syntax (3 stale references — file was newly tracked in PR #333 with mixed ping.sh/notify.sh syntax; per PR-A doctrine this should have been caught by the same sed pattern); **(2)** `docs/decisions/ADR-0042-orchestrator-role.md` References section (lines 7, 13, 61, 68) references "PR #332 (PR-B)" but PR-B is PR #333 (post-split) — 4 stale PR-number references; **(3)** `docs/decisions/INDEX.md` does NOT list ADR-0042 (INDEX update was in architect scope per file ownership matrix, not done in PR #333); **(4)** ADR header reads `# ADR-41:` but filename was `ADR-0041-orchestrator-role.md` (header/filename inconsistency, breaks grep-by-filename convention); **(5) [discovered in PR #334 rebase]** numbering collision — `ADR-0041-event-model-v8-verdict-posted.md` (PR #323) claimed the same number. **Per ADR-0031 (owner-override doctrine)**, the override is legitimate — owner authority on `.claude/` (per file ownership matrix) and on ADR acceptance (per CLAUDE.md) is unconditional. **Per doctrine "never let 'we'll fix it later' leave a meeting without a tech-debt ticket"**, the unaddressed findings MUST be documented. **PR #334 (amended)** addresses Findings 2/3/4 architect-fixable items + Finding 5 (renumbering Option A: file renamed `ADR-0041-orchestrator-role.md` → `ADR-0042-orchestrator-role.md`, header updated `# ADR-0041:` → `# ADR-0042:`, INDEX.md row updated). **Finding 1 is owner-only** (`.claude/` is human-only territory per file ownership matrix) — Issue #335 filed (owner applies the 2 sed lines). **Severity**: **M** → **M-** (Findings 2/3/4/5 resolved in PR #334 amendment; only Finding 1 remains — owner-only, Issue #335). **Payoff trigger**: (i) Sprint 5 retro — review if owner-override pattern on architect findings becomes routine (>2 occurrences/sprint) and consider ADR-0031 amendment (require post-override follow-up PR for non-blocking findings); (ii) any future soul-sed cycle will re-surface Finding 1 (the sed will catch the 3 stale refs but won't auto-migrate them); (iii) ADR renumbering doctrine → consider INDEX.md §Conventions amendment for "ADR-numbering-collision" detection at PR-creation time (similar to label-check). **Architectural lesson**: when owner overrides architect 🟡 SUGGESTION findings (non-blocking) on a merged PR, the override creates **silent tech debt** unless an explicit follow-up PR is filed. The 2-tier architect review taxonomy (ADR-0031) treats 🟡 as "owner may override freely" — but "freely" should not mean "without follow-up". Recommend ADR-0031 amendment in Sprint 5: 🟡 overrides MUST have either (a) follow-up PR filed by architect within 24h, or (b) explicit rationale in PR body (e.g., "Findings accepted as known debt"). **Lesson 2 (from Finding 5 discovery)**: ADR numbering has no cross-PR coordination gate — two PRs can independently claim the same number if merged in different sprints without coordination. Recommended: label-check workflow extension to validate ADR-numbering uniqueness across `docs/decisions/ADR-*.md` at PR-open time. | PR #333 (squash-merged 955ab57 at 2026-06-24T08:29:13Z); architect verdict cmt 4787180967 (2026-06-24T08:08:16Z); branch `feat/orchestrator-role-and-adr-41`; 3 files (.claude/agents/orchestrator.md +244, .gitignore -1, docs/decisions/ADR-0041-orchestrator-role.md +72 → renamed to ADR-0042 in PR #334 amendment); sister PR #332 (PR-A, MERGED 77829e1 at 2026-06-24T08:05:59Z — split executed per architect Option A recommendation); Issue #320 (soul-side bug, partially closed by #332 + #333); Issue #335 (Finding 1 owner-only — orchestrator.md sed migration); Issue #340 (Finding 5 numbering collision, resolved via PR #334 amendment Option A); PR #323 (Event Model v8, ADR-0041-numbering-collision source); PR #334 (Findings 2/3/4/5 resolution, amended 2026-06-24T10:47Z) | **M-** (Findings 2/3/4/5 RESOLVED in PR #334 amendment; Finding 1 remains owner-only) | Sprint 5 retro: review owner-override pattern on architect findings; possible ADR-0031 amendment requiring 🟡 override follow-up PR or explicit acceptance rationale; ADR-numbering-collision gate (label-check extension) to prevent Finding 5 recurrence; owner applies Finding 1 sed via Issue #335 | @architect (filed + PR #334 amendment + ADR-0031 amendment proposal + ADR-numbering-collision gate proposal), @atilcan65 (decides whether to apply Finding 1 sed via Issue #335; applies ADR-0031 + ADR-numbering-collision amendments if approved in Sprint 5), @developer (out of scope for this debt — no code change), @tester (no regression — docs-only debt) |
| TD-027 | **`wake_nudge` event IDs lack `b<bucket>` suffix — TTL trim filter cannot expire them, ring becomes dominated by wake_nudges over time** (NEW, P0 #345 RCA sister). On 2026-06-24T13:16Z, while architect-verdicting P0 #345 (Issue #345, agent-watch.sh dedup ring frozen), 4th suspect surfaced beyond dev's 3-bug triage: `scripts/agent-state.sh:255-266` TTL trim filter retains IDs that do NOT match the `b[0-9]+$` pattern via the `else .` branch (comment says "wake_nudge / pr-merged / pr-review — retain"). `wake_nudge` IDs are formatted `wake-nudge-<role>-<iso-ts>` with **no bucket suffix** — they fall through to retention. After P0 #345 fix lands (Fix 1 atomic batch mark + Fix 2 flock + Fix 3 cap 50→200), mark will work correctly, but the ring will accumulate wake_nudges **indefinitely** (~1440/day/role = 1/min × 60 × 24). At 200 cap, trim keeps the LAST 200 wake_nudges, never expires them; over 2-4 weeks, the ring becomes dominated by wake_nudges with little room for real events. **Effect** (post P0 #345 fix, 2-4 week horizon): dedup effectiveness degrades as wake_nudges evict real events from the ring → wake_nudge itself becomes re-emittable (matches existing ring entries are filtered, new ones are not, but the SLOT is taken) → effectively: stale wake_nudges persist forever. **Not the FREEZE cause** (that's Bug 4 concurrent race + Bug 1 historical stringification per dev's triage), but a **slow-burn tech debt** that will cause similar dedup issues in 2-4 weeks if not addressed. **Fix options**: (a) **add bucket suffix to wake_nudge emission** at source (likely `scripts/agent-watch.sh` near the `query_wake_nudge` or equivalent function — need to grep) → format `wake-nudge-<role>-b$(date -u +%s)/300` so TTL trim CAN expire them; minimal code change, single line. (b) **drop wake_nudges from dedup entirely** (they're synthetic, throttle-bounded; can be re-fired without harm) — change filter to NOT include wake_nudge IDs in the ring at all (no mark call for wake_nudge kind); more invasive but cleaner semantically. (c) **bypass TTL filter for wake_nudges in trim** — change cmd_trim to actively expire wake_nudges older than N hours (separate age check beyond bucket suffix). **Recommendation**: option (a) — minimal change, preserves all existing semantics, lowest blast radius; option (b) requires deeper audit of wake_nudge consumers. **Severity**: **L** (slow burn; not the P0 FREEZE cause; 2-4 week horizon; throughput tax only). **Payoff trigger**: Sprint 5 P2 capacity opens AND P0 #345 fix lands successfully (Fix 1+2+3 GREEN) AND d036 silent-failure watchdog observes wake_nudge ratio in ring > 50% for > 7 consecutive days (new watchdog TU10 candidate). **Architectural lesson**: when adding TTL filters to a ring buffer, ALL ID formats must be uniformly bucket-suffixed OR the TTL filter must explicitly handle non-bucket IDs with an alternative age criterion. The current `if test("b[0-9]+$") else .` is a footgun — non-bucket IDs are silently exempted from expiry. Filed per architect observation during P0 #345 RCA (2026-06-24T13:22Z verdict cmt 4789754272, dev's triage cmt 4789715041). | Issue #345 (P0, RCA cited); dev triage cmt 4789715041 (3-bug analysis on P0 #345); architect verdict cmt 4789754272 (4th suspect Suspect #3 added); `scripts/agent-state.sh:255-266` (TTL trim filter); wake_nudge emission site TBD in `scripts/agent-watch.sh` (likely `query_wake_nudge` function); sister d036 regression spec (TU7 silent-failure watchdog enhancement) | **L** (slow burn; not FREEZE cause; 2-4 week horizon; throughput tax only; not data-corrupting) | Sprint 5 P2 (after P0 #345 fix lands GREEN); option (a) implementation ~0.25 SP (single-line format change + d036 TU10 new); option (b) ~1 SP (audit wake_nudge consumers + change filter); option (c) ~0.5 SP (cmd_trim wake_nudge age branch) | @architect (filed + P0 #345 verdict d036 spec architect), @developer (option a impl — single-line wake_nudge format change in agent-watch.sh), @tester (d036 TU10 wake_nudge ratio in ring regression), @atilcan65 (no action — agent-fixable; Sprint 5 prioritization decision) |
| TD-030 | **PR #358 §Risks R2 mitigation factually wrong on two counts — missed `.gitignore` auto-gen files + canonical-path live-state assumption** (NEW, P1 path, sister to TD-028/TD-029, **8th in family**, **4th post-merge**). On 2026-06-24T19:08Z, while architect-verdicting dev's P1 incident #363, dev discovered PR #358 R2 mitigation was wrong: (1) **27 refs to `/home/atilcan/projects/AtilCalculator` in `scripts/.tmux-bootstrap/*.sh` are gitignored** (`.gitignore` line 67) and auto-generated by `scripts/dev-studio-start.sh` line 27 dynamic `REPO_ROOT` resolution; the grep was correct but my interpretation was wrong — refs are auto-gen snapshots, not source-controlled files; (2) **canonical runner path `/home/atilcan/actions-runner/_work/...` does not exist** at design time (owner was already mid-migration Steps 6+7), so my "post-migration REPO_DIR=$GITHUB_WORKSPACE" was a designer assumption not verified against live state. **Architect review (8-lens) at 16:25Z passed**: (a-i) all green. **Missed**: `.gitignore` enumeration is not part of any current lens; live-state verification (`ls -la` / `ps -ef` / `git log --oneline -5`) is not a pre-merge gate either. **Pattern**: same family as TD-016/018/019/020/028/029 — agent verified LOCAL SHAPE but missed cross-cutting concern (this time: **auto-gen content boundary** + **live-state drift**). **Differentiation from TD-028/TD-029**: TD-028/TD-029 were about platform constraints (SHA pin, GA path sandbox) caught by extending checklist with lenses (h) + (i); **TD-030 is about REPO CONTENT (auto-gen files) + LIVE STATE (host filesystem), caught by extending checklist with NEW lens (j)**. **Fix**: extend architect soul checklist from 8-lens (ADR-0043) to **9-lens** with item **(j) auto-generated file refs + live-state verification pre-publish gate** — when proposing ANY edit that touches a path/secret/service/runtime artifact, the architect MUST: (1) enumerate auto-gen files explicitly (grep `.gitignore` + Makefile + pyproject.toml for `auto-gen` / `bootstrap` / `regenerate` markers; document regeneration trigger + idempotency contract); (2) verify live-state for canonical-path assumptions (`ls -la` / `ps -ef` / `git log --oneline -5` at design time); (3) document both in a TD-030 attestation block in §Risks (mirrors ADR-0027 §Threat model attestation). **Severity**: **M** (P1 contained via Option A no-op + system self-heal via `dev-studio-start.sh` regen with dynamic REPO_ROOT; no merge damage; same severity as TD-028/TD-029; 8th in family, 4th post-merge — pattern is established). **Payoff trigger**: Sprint 6 P2 — architect soul amendment "(j) auto-generated file refs + live-state verification pre-publish gate" (~0.5 SP, agent-fixable, owner-gated per file ownership matrix). Optional Sprint 7+: extend `d040-deploy-path-guard.sh` + `d041-platform-constraint-linter.sh` to include (j) sub-categories. **Architectural lesson (4th post-merge, 8th in family)**: pre-publish gates must include **repo content boundaries** (what's source vs auto-gen) AND **live-state drift** (what's actually on the host at design time), in addition to platform constraints + ADR constraints + local shape. The 9-lens checklist is necessary when designing cross-cutting changes (infra, deploy, paths, secrets). Codified in **ADR-0045**. | Issue #363 (P1 INCIDENT, opened 2026-06-24T22:00Z by @developer); PR #358 (MERGED ddfd43f, design with wrong R2); PR #361 (MERGED c9268ba, impl); owner Steps 6+7 executed 21:55-56Z (symlink unlink + runner dir removal); architect decision on #363 cmt 4792738456 (2026-06-24T19:08Z, Option A recommended); ADR-0045 (codification, this PR) | **M** (P1 contained; 8th in family, 4th post-merge; soul amendment is agent-fixable, owner-gated) | Sprint 6 P2 — architect soul amendment "(j) auto-generated file refs + live-state verification pre-publish gate" (~0.5 SP, owner-gated per file ownership matrix); ADR-0045 codification (this PR); PR #358 §Risks R2 amendment follow-up commit (architect-fixable, low-priority) | @architect (filed + owns the miss + ADR-0045 author + soul amendment author), @developer (dev lane for #363 Option A docs PR + #194 follow-up), @tester (d043 auto-gen enumeration + live-state verification regression spec), @human (soul amendment owner gate) |

### Blind-spot family (cross-reference: TD-016 + TD-018 + TD-019 + TD-020 + TD-028 + TD-029 + TD-030)

Eight TDs across Sprint 3 + Sprint 4 + Sprint 6 are all instances of the same class — **agent (architect or orchestrator) issued a verdict/guidance after verifying local shape but before tracing end-to-end contract / platform constraints / repo content / live state**. The lesson generalizes: **before issuing a verdict, an agent MUST trace the full chain from source to sink** — this includes (a) static data flow (TD-016), (b) runtime preconditions (TD-018), (c) canonical-entry verification (TD-019), (d) identity / privilege boundary, (e) service-state (where service actually lives, not just how to call it), (f) instance-vs-pattern (ADR documents pattern, host has instance — verify the instance matches), **(g) silent-skip preflight pattern (NEW, TD-020) — any preflight step that WARN-only-skips on missing preconditions is a structural risk**, **(h) workflow YAML pre-publish gate (TD-028) — preserve SHA pins exactly per ADR-0027 §Threat model**, **(i) platform hard constraints (TD-029) — verify against platform sandbox/security/runtime constraints (e.g., GA's `path:` MUST be under `_work/`, k8s `securityContext`, systemd `ProtectSystem`) before designing the value**, **(j) auto-generated file refs + live-state verification (TD-030, NEW) — enumerate `.gitignore` auto-gen files + verify live-state of canonical-path assumptions (`ls -la` / `ps -ef` / `git log --oneline -5` at design time); document both in TD-030 attestation block in §Risks (mirrors ADR-0027 §Threat model)**. Together these form the **9-lens architect review checklist** (Sprint 6 P2 soul amendment target per ADR-0045).

| TD | Agent | Class | What was missed | Severity |
|----|-------|-------|-----------------|----------|
| TD-016 | architect | static data flow | REPO_DIR path + TELEGRAM env binding | M |
| TD-018 | architect | runtime preconditions | systemctl D-Bus bus + empty secret values + early-exit silent rollback skip | M |
| TD-019 | orchestrator | canonical-entry verification | hallucinated `atilcalc.web.app:app` vs canonical `atilcalc.api.main:app` | M |
| TD-020 | architect | silent-skip preflight (NEW lens) | `.venv` missing → preflight WARN-skip → restart fails on missing `uvicorn` | M |
| TD-021 | architect + human | PR label drift script (NEW family) | 4-mutation signature (`-status:in-review -cc:tester -needs-tester-signoff +status:ready`) fires prematurely — sets `status:ready` BEFORE tester review. **Now affects P0 PRs** (3rd instance on PR #161 at 2026-06-20T06:29:03Z, RCA-9 fix-PR). See Issue #125 for full pattern; PR #157 (2nd instance) + PR #161 (3rd instance) + PR #122 (1st instance, 3x) + **PR #736 ONBOARDING.md (6th instance, 2026-06-30T20:30:45Z, type:docs + no wake labels initial-trigger pathology, Issue #744)** + **PR #745 ADR-0048 amend-3 (7th instance, 2026-07-01T04:25:40Z, same initial-trigger pathology in real-time — orch flag cycle ~#2538 confirms; tester verdict landed 04:27:17Z AFTER auto-promote, owner authority precedent from PR #736 revert cycle #2515 preserved — NO state correction applied)** = 7 total occurrences across 6 days (2026-06-20 → 2026-07-01). **Resolution path**: ADR-0048-amendment-3-initial-trigger-verdict-state-guard.md (proposed 2026-07-01, closes Issue #744) + d077-TC6 d-test extension + `.github/workflows/label-check.yml` Layer 5 verdict-state guard (~40 LoC yaml delta, owner merge per file ownership matrix). PR #677 (Issue #675 fix, Amend-1) closed instances 1-5 re-fire-loop family only; PR #736 (6th) and PR #745 (7th) are the FIRST + SECOND initial-trigger variants and trigger Amend-3 codification. | **H** (P0 path) |
| TD-022 | architect + developer | Layer 3 scope gap (Issue #227 + ADR-0035 residual). On 2026-06-22T10:30Z, owner removed `cc:tester` + `needs-tester-signoff` + flipped `status:in-review` → `status:ready` on PR #245 (reopened, head `da7391a`). This triggered a `label_change` event → Layer 3 (`.github/workflows/label-check.yml`) fired on the **label-change path**, not the `opened` path → CI RED (label-check failure). Per ADR-0035 (MERGED 06:07:33Z via PR #229), Layer 3 step has `if: github.event.action == 'opened'` filter — but this **only suppresses on NEW PR opens, NOT on label-change events on existing/reopened PRs**. **Residual gap**: any `label_change` (add/remove) on a reopened/relabeled PR still triggers Layer 3's `cc:tester` + `needs-tester-signoff` invariant check, failing on otherwise-correct PRs. Same class as TD-021 (label-drift → CI RED) but on the **gate trigger** side, not the drift script side. **Fix options**: (a) extend `if:` filter to also exclude `label_change` (or only check on `opened` + `reopened`); (b) split Layer 3 into two workflows — one for `opened` (strict 4-cat invariant + type-driven requirements) and one for `label_change` (advisory only, posts comment but does not fail); (c) introduce a `label-check-allow-override` label that, when present, exempts the PR from Layer 3 strict checks (owner-applied only, requires `verdict-by:<ts>`). **Recommendation**: option (a) — minimal change, single-line workflow YAML edit + d023 regression. **Severity**: **M** (CI RED only; no merge damage; PR #245 closed as redundant by owner 10:39:53Z before fix needed; but pattern will recur on any reopened/relabeled `type:bug` PR). **Architectural lesson**: ADR-0035 scope was too narrow — `if: github.event.action == 'opened'` only handles ONE trigger path; label_check workflow should consider the full event matrix (`opened`, `reopened`, `labeled`, `unlabeled`). Filed per architect observation during PR #245 close. | PR #245 (reopened 10:31:39Z, owner label change 10:30Z, CI RED 10:33Z, closed 10:39:53Z as redundant with PR #246 MERGED) | **M** (CI RED on otherwise-correct PR; pattern will recur on any reopened/relabeled `type:bug` PR; no data loss; throughput tax) | Owner + architect decide (a) / (b) / (c); option (a) implementable in Sprint 4 retro (~0.25 SP, single-line workflow YAML + d023); option (b) more invasive (~1 SP, workflow split); option (c) requires label-taxonomy decision | @architect (filed), @developer (workflow YAML fix if a/b selected), @tester (regression test d023 if option a), @atilcan65 (decide path + approve workflow file change — `.github/workflows/` is human-only per file ownership matrix) |
| TD-030 | architect | auto-gen file refs + live-state drift (NEW lens) | PR #358 R2 missed `.gitignore` bootstrap files + canonical-path live-state assumption | M |

**Consolidation plan** (RETRO-003, 2026-06-20T18:34Z): replace TD-016/TD-018/TD-019/TD-020 with a single "blind-spot family" entry + **7-lens architect review checklist** (a-g). PM drafts retro doc; architect contributes the consolidated doctrine + soul-file amendment. **TD-021** (drift script family) is SEPARATE from the blind-spot family — it's a tooling/automation bug, not a review lens gap. RETRO-003 should add a parallel "drift detection" track: anti-drift clause in `.claude/CLAUDE.md` + optional `d021-drift-detector.sh` CI lint.

**Consolidation plan** (RETRO-005, Sprint 5 retro, scheduled ~2026-06-25): per ADR-0045, extend checklist from 8-lens to **9-lens** by adding **(j) auto-generated file refs + live-state verification**. TD-030 is the 8th family instance + 4th post-merge — pattern is established. Sprint 6 P2 scope: soul amendment "(j) auto-gen + live-state" (architect proposal, owner gate); d043 regression test (tester); RETRO-005 #17 candidate per Issue #363. Sister to TD-028 + TD-029 closeouts.

## Resolved items

| ID | Description | Resolved | Resolver |
|----|-------------|----------|----------|
| TD-009 | Multi-agent branch-contention (2nd instance of TD-003). Resolved via per-agent git worktrees — `atilcalc-{architect,developer,tester}` at /home/atilcan/projects/, each with own cwd. See [`docs/sprints/sprint-01/td-009-resolution.md`](sprints/sprint-01/td-009-resolution.md) for setup + operational contract. | 2026-06-18 | @orchestrator (implementation), @atilcan65 (decision), @architect (this doc update) |
| TD-028 | Pre-PR detection of moving-tag `actions/checkout@v4` in architect's deploy.yml draft (Issue #347). Doctrinally closed by [ADR-0043](./decisions/ADR-0043-8-lens-architect-review-checklist.md) lens (h) — workflow YAML pre-publish gate (preserve SHA pins per ADR-0027 §Threat model). Soul file amendment (architect.md §Standard Workflows (h) lens) pending owner action per file ownership matrix — non-blocking. | 2026-06-24 | @architect (ADR-0043 PR #356) |
| TD-029 | P0 #351 — PR #350 Option C `path:` override violated GA sandbox. Closed via PR #352 revert (deploy chain restored) + PR #354 (TD-029 docs). Doctrinally closed by [ADR-0043](./decisions/ADR-0043-8-lens-architect-review-checklist.md) lens (i) — platform hard constraints pre-publish gate (8 sub-categories: `path:` under `_work/`, `runs-on:` labels, `permissions:` scope, `timeout-minutes:`, `concurrency:` group, `if:` event scoping, secret existence, platform-specific sandbox). Soul file amendment (architect.md §Standard Workflows (i) lens) pending owner action per file ownership matrix — non-blocking. Sprint 5 P1 #193+#194 redesign (Option B') applies (i) from day 1. | 2026-06-24 | @orchestrator (PR #352 revert + PR #354 TD-029 doc), @architect (ADR-0043 PR #356) |

## Process notes (operational discipline)

1. **Every label flip MUST be followed by a `gh pr view N --json labels` verification query.** Never trust the command output alone — `gh pr edit` silent failures are a known artifact (TD-004).
2. **Every ADR / design doc / tech-debt edit MUST be followed by a `git status` check** to confirm the file is in the working tree (or committed, depending on intent). Multi-agent branch contention (TD-003) can cause edits to land on the wrong branch or be lost.
3. **`gh pr edit` with multiple --add-label + --remove-label in one call is unreliable.** Use separate calls for atomicity.
4. **Cross-agent label state is not stable.** After a label flip, another agent's automation or workflow may re-add labels. Re-verify before assuming the flip stuck.

| TD-031 | architect | rubber-stamp pattern + L337 syntactic regression (NEW family) | PR #438 v2 (commit b9aa72d) hotfix dropped closing backtick at L337 audit body, JS SyntaxError on every PR opened post-merge. 2nd instance of arch+tester rubber-stamp in 24h (1st: PR #434 Layer 5 TypeError, 2nd: PR #438 L337 syntax). Content-anchor d-test (d048 TC5/TC6) missed both regressions — catches string presence, not JS syntax or runtime behavior. **Doctrinal fix**: ADR-0049 d050b behavioral workflow test framework (workflow_dispatch + mock PR payload + behavioral assertion) + 9-Lens sub-check **(k) JS syntactic correctness** (NEW, `node --check` on extracted `actions/github-script` snippet). | PR #438 (b9aa72d MERGED 14:39:27Z, SyntaxError observed 14:42Z on PR #435 /recheck), Issue #441 (P0 hotfix), Issue #440 (P0 d050b framework), ADR-0049 (P0 proposed) | **H** (P0 cascade, blocks all non-docs PRs at CI label-check) | Sprint 12 P1 (ADR-0049 d050b ~3.0 SP). Architect lane: 9-Lens sub-check (k) added to architect.md §Code review (TD-031 follow-up). | @architect (filed + ADR-0049 + 9-Lens update + Issue #441 RCA), @tester (Issue #440 d-test contract + Issue #441 RCA + Issue #440 P0 escalation), @developer (Issue #441 hotfix branch `hotfix-issue-441-layer4-backtick` + d050b impl), @atilcan65 (Issue #441 hotfix owner squash + d050b-dispatch.yml owner squash) |
| TD-032 | architect | Sprint 16 P1 doctrine hardening workshop — workflow YAML carry-forward (3 items deferred to owner merge) | Sprint 16 P1 workshop (PR #562 MERGED @ 781038a) surfaced 3 workflow YAML carry-forward items: **(1)** `closes-anchor-check.yml` (NEW, per ADR-0057 §Workflow YAML guard) — parser-friendly Closes anchor lint with `silent_skip` log on unrecognized format (WARN not FAIL); **(2)** `status-label-to-board.yml` amendment (per ADR-0058 §Workflow YAML guard) — verdict signature guard (`if:` comment body regex), multi-fire prevention (per-PR fire-count + 60s debounce), stability gate (60s cooldown), stale-cache check (5min threshold + `silent_skip`); **(3)** ADR-0058 amendment candidate "Layer 5 idempotent label DELETE" (per dev turn=091 observation on PR #562) — codifies the cheaper-fix pattern: Layer 5 DELETE 404 → silent_skip log → idempotent re-run on next labeled event. **All 3 items deferred per file ownership matrix** (`.github/workflows/` = human-only territory, architect + tester draft, owner merges per ADR-0031 owner-override doctrine). | Sprint 16 P1 doctrine hardening workshop (PR #562 squash @ 781038a, 2026-06-27T22:12:31Z), Issue #546 (Sprint 16 P1 AC1) + Issue #560 (AC1+AC2) both CLOSED via `Closes #` parser-friendly anchor (doctrine self-validation), §32 NEW LIVE INSTANCE ledger #6 captured (PR #562 Layer 5 DELETE 404 → ADR-0056 idempotency reconcile self-corrected in 10s) | **M** (workshop doctrine hardened via ADR-0056/0057/0058; workflow YAML guards = belt-and-suspenders, NOT blocking) | Sprint 16 P1+ (after owner squash gate per file ownership matrix). Trigger: when Layer 5 false-positive rate exceeds 1/week OR when sprint boundary work allows owner merge. | @architect (filed, ADR-0057 + ADR-0058 doctrine), @tester (workflow YAML draft co-author + d062/d063 candidate test contracts), @developer (d062-closes-anchor-parser.sh + d063-comment-trigger-guard.sh candidates + ADR-0058 amendment candidate surfacing), @atilcan65 (owner merge gate per file ownership matrix + ADR-0031) |

| TD-033 | architect | ADR-0060 §2 step 2 regex misses markdown task-list form `- [x] AC\d+:` (cycle 740 OVERDUE — heartbeat logged but file never updated; filed cycle 751 with drift correction) | PR #615 ADR-0060 §AC mapping verification doctrine, cmt 4826994329 (dev peer review, 2026-06-28T18:24:06Z) + arch ACK cmt 4827005025 (18:28:30Z). Dev flagged: ADR-0060 §2 step 2 regex `^- \*\*AC\d+\*\*` or `^- AC\d+` does NOT match markdown task-list form `^\s*-\s*\[([ x])\]\s+AC\d+:`. Workaround applied in PR #616 (canonical AC appendix uses `**AC1**/` form). Sprint 19+ doctrine-script enhancement needed. Suggested regex: `^\s*-\s+(?:\[([ x])\]\s+)?\*?\*?AC\d+\*?\*?[:.]/` or simpler `^[\s]*-\s*\[([ x])\]\s+AC\d+[:.]/`. **Heartbeat drift correction**: cycle 740 heartbeat logged "TD-033 filed" but the actual tech-debt.md entry was NOT appended at that time; verified cycle 751 via `grep TD-033 docs/tech-debt.md` returning 0 matches. Filing now to restore ledger accuracy. Sister-pattern: TD-031 (rubber-stamp pattern). | Sprint 18 P1 (cluster), 2026-06-28 cycle 740 logged + cycle 751 actualized | **M** (doctrine-script gap; PR #616 workaround applied; non-blocking; Sprint 19+ candidate ~0.5 SP) | Sprint 19+ P2 (after P0/P1 cluster ships). Trigger: when ADR-0060 doctrine is applied to ≥3 stories in same sprint AND ≥1 AC item uses markdown task-list form. | @architect (filed + drift correction), @developer (regex enhancement + d-test extension), @tester (regression test for new regex), @atilcan65 (no action — agent-fixable) |
| TD-034 | architect | Sprint 18 P0 cycle 750 misread of tester's BUG-3 flag — re-added `cc:developer` that was correctly stripped per ADR-0015 terminal handoff pattern (NEW) | Cycle 750 (2026-06-28T19:03:30Z) ack-pinged tester that I re-added `cc:developer` to PR #615 per their BUG-3 flag (cmt 4827091151). Cycle 751 (19:04:59Z) pr_labeled event showed `cc:developer` STRIPPED AGAIN within 89 seconds — TC4 reversal handler re-fired on my `labeled` event and stripped it. **Root cause analysis** (cycle 751, deeper read of dev lane verdict cmt 4827088693): dev lane verdict explicitly stated "Removing cc:developer label in next command (dev lane role done; arch owns PR lifecycle)" — so dev INTENDED to remove `cc:developer` per ADR-0015 terminal handoff pattern. Tester's BUG-3 flag was correctly BUG-3-compliant (per ADR-0009 §10.3 — tester does NOT add labels not theirs to add), but the underlying assumption (strip was an error) was incorrect. **Cascade-strip is BY-DESIGN**, not a BUG-3 false positive. My cycle 750 re-add was an OVERCORRECTION that violated the standard terminal handoff template. **Lesson**: (1) When tester's BUG-3 flag triggers a label-strip investigation, FIRST re-read peer comments (especially terminal handoff statements) BEFORE re-adding labels that bots strip. (2) `cc:developer` absent at `status:ready` is BY-DESIGN per ADR-0015 §Tipik handoff zinciri: "Developer PR aç → remove cc:developer, add needs-tester-signoff" — at status:ready (terminal), cc:developer is correctly absent. (3) The cascade-strip loop (re-add → re-strip within ~90s) is an operational signal that the re-add is wrong, not a bug to fight. **Sister-pattern**: TD-007 (architect posted review before reading peer's blockers — same class: missed context before action). **Operational impact**: zero (cluster at owner squash gate regardless; no merge damage; no agent throughput tax long-term). **Action**: ack tester with corrected understanding; do NOT re-add cc:developer again; PR #615 state at cycle 751 (cc:developer absent) is correct per standard template. | PR #615 cycle 750 ack (cmt 4827091151 misinterpretation) + cycle 751 pr_labeled (19:04:59Z, cc:developer stripped); cmt 4827088693 dev lane verdict (terminal handoff intent); ADR-0015 §Tipik handoff zinciri | **L** (lesson learned; no merge damage; no data corruption; cluster at owner squash gate regardless; self-correction within 1 cycle) | Sprint 18 P1 retrospective (RETRO-013 candidate). Trigger: when architect misreads a BUG-3 flag from tester AND cascade-strip loop fires (operational signal that re-add is wrong). | @architect (filed + ack-tester correction cycle 751), @tester (no action needed; tester was BUG-3 compliant), @developer (no action needed; dev lane verdict was correct), @atilcan65 (no action needed; cluster at owner squash gate) |
| TD-035 | architect | Cycle 751 "drift correction" itself drifted — `docs/tech-debt.md` edited but never committed/pushed; second-order heartbeat drift (NEW) | Cycle 751 (2026-06-28T19:05-19:10Z) appended TD-033 + TD-034 to `docs/tech-debt.md` as a "drift correction" for the cycle 740 OVERDUE filing. Cycle 766 (2026-06-28T19:52:13Z, post-REPRIME) re-verified ground truth and discovered **main `docs/tech-debt.md` only contains up to TD-032** — neither TD-033 nor TD-034 made it to main. Root cause: cycle 751 edits were made to working tree on branch `fix/sprint-17-close-md-cluster-lag-correction` (PR #619's branch), but never `git add`ed/committed/pushed. PR #619 was squash-merged at 2026-06-28T19:47:06Z (commit `39f6772`, on the file `docs/sprints/sprint-17/close.md` only) — taking the branch with it. Uncommitted working-tree edits to `docs/tech-debt.md` were orphaned. **Recovery** (cycle 766): `git stash push` → `git checkout main` → `git pull --ff-only` (3 commits behind) → new branch `fix/sprint-18-tech-debt-drift-recovery` → `git stash apply` → TD-033+TD-034+TD-035 commit → PR open. **Lesson**: heartbeat-driven file edits MUST complete the tight loop `edit → git add → git commit → git push → verify-on-remote` atomically. Skipping ANY step causes silent drift. **Sister-pattern**: TD-031 (rubber-stamp — different mechanism, same silent-failure mode), TD-033 (cycle 740 first-order drift). **Operational impact**: zero today (Sprint 18 cluster at owner squash gate regardless), but recurring pattern (3rd heartbeat-drift class instance across cycles 740-751-766) is a CLASS that warrants soul-level fix. **Recommended doctrine** (Sprint 18 retro candidate): add to architect.md §Operating Principles "8. **Heartbeat tight loop** — every `docs/tech-debt.md` / `docs/decisions/ADR-NNNN-*.md` / `docs/designs/STORY-NNN-design.md` edit MUST be followed within the same work unit by `git add <file> && git commit -m '...' && git push` AND `git status` verification. Heartbeat claim without push = ledger untruth." | PR #619 squash (39f6772, 2026-06-28T19:47:06Z) on branch `fix/sprint-17-close-md-cluster-lag-correction`; cycle 766 REPRIME re-verification via REST API (`/repos/.../contents/docs/tech-debt.md?ref=main`) showed TD-033/TD-034 absent from main; TD-031 + TD-033 + this entry = 3rd heartbeat-drift class instance | **M** (recurring class — 3rd instance within 1 day; pattern warrants soul-level fix; current instance zero-impact because cluster at owner squash gate) | Sprint 18 retrospective (RETRO-013 candidate) — propose soul amendment to architect.md §Operating Principles + companion d-test (d069 heartbeat-tight-loop-regression-guard — verifies `docs/tech-debt.md` claims appear in `main` within N cycles of heartbeat assertion). Trigger: when 3rd+ instance of heartbeat-drift class recurs in same sprint. | @architect (filed + drift recovery PR), @tester (no action needed for this entry; d069 candidate test contract if Sprint 18 retro prioritizes), @developer (no action needed), @atilcan65 (no action needed; agent-fixable doctrine) |
| TD-037 | architect | Auto-Verdict-By hook TypeError — workflow-side silent-skip preflight blind spot (NEW, PR #712 LIVE INSTANCE, 9-Lens blind-spot family 11th instance) | Cycle ~1587 (2026-06-30T10:01:27Z) on PR #712 (STORY-S21-006), `label-check` workflow run #28436203805 failed at Auto-Verdict-By hook step 8:33 with `TypeError: Cannot read properties of undefined (reading 'label')` — hook reads `context.event.label.name` but workflow fires on `opened`/`reopened` events where `context.event.label` is undefined. **Confirmed pattern**: 3 label-check runs on PR #712 — 2 FAIL (on opened/reopened), 1 PASS (on labeled event) — hook is **intermittently broken depending on trigger event**. **Architect review was correct**: 9-Lens architectural review of PR #712 content = 🟢 OK (5 .tmpl files, .gitignore contract, d096 5/5 GREEN). Step 4 §CI-verdict-timing gate held the verdict (DEFERRED) because `label-check` was FAIL — exactly as doctrine intends. **Root cause** (workflow-side, NOT PR-side): `.github/workflows/label-check.yml` Auto-Verdict-By hook (ADR-0024 amendment §Path 1) assumes `context.event.label.name` exists. Fix: guard with `context.payload.label?.name` (optional chaining) OR add `if: github.event.action == 'labeled'` condition. **Same class as TD-018** (architect missed runtime preconditions) + **TD-020** (silent-skip preflight WARN-only pattern) — the Auto-Verdict-By hook is a **silent-skip preflight** that crashes instead of skipping (different failure mode but same blind spot). Per file ownership matrix `.github/workflows/` = **human-only territory** — architect flags, owner fixes. **Severity**: **M** (workflow-side bug; PR cycles blocked until fixed; ~5-10 min owner fix; sister-pattern to TD-022 Layer 3 scope gap — workflow YAML event-matrix coverage). **Payoff trigger**: when (i) owner applies the 1-line fix to Auto-Verdict-By hook (guard or `if:` filter), AND (ii) companion d-test added — `d078-layer-5-auto-verdict-by-event-guard.sh` candidate (workflow_dispatch + mock event payload + assertion that hook returns cleanly on opened/reopened events). **Architectural lesson (11th in 9-Lens blind-spot family, NEW)**: workflow YAML event-matrix coverage is a pre-publish gate item that architects MUST verify when reviewing PRs that touch workflow files OR depend on workflow behavior. Extends lens (i) platform hard constraints with sub-category: every `actions/github-script` step MUST verify all event-types in the workflow's `on:` matrix. Currently lens (i) covers 8 sub-categories per ADR-0043; this would be sub-category 9. **Sister-pattern**: TD-022 (Layer 3 `if:` filter too narrow — only handles `opened` not `label_change`), TD-031 (L337 backtick JS syntax), TD-035 (heartbeat drift). Filed per architect observation cycle ~1587 post PR #712 verdict cmt 4842207495. | PR #712 (head 3791cff, squash pending); workflow run #28436203805 (FAIL 10:01:27Z, TypeError L8:33); workflow run #28436551940 (SUCCESS 10:07:18Z, intermittent); workflow run #28436552023 (FAIL 10:07:31Z, intermittent); arch verdict cmt 4842207495 (DEFERRED, Step 4 doctrine applied); arch peer-poke cycle ~1587 (owner + dev + orch notified); ADR-0024 amendment §Path 1 (Auto-Verdict-By hook spec origin); ADR-0043 lens (i) (platform hard constraints, 8 sub-cats — sub-cat 9 candidate) | **M** (workflow-side bug; blocks PR cycles intermittently; sister to TD-022 Layer 3 scope gap; 1-line owner fix; d-test candidate unblocks architectural lesson codification) | Sprint 23+ P2 (after owner workflow fix + d078 candidate GREEN). Owner applies 1-line fix to `Auto-Verdict-By hook` in `.github/workflows/label-check.yml` (5-10 min, human-only territory per file ownership matrix). Tester authors d078 regression (~0.5 SP). Optional: ADR-0043 amendment adds sub-cat 9 to lens (i) — workflow YAML event-matrix coverage. | @architect (filed + PR #712 verdict held per Step 4 + peer-poke escalation), @tester (d078 candidate test contract — workflow_dispatch + mock event payload + assertion), @developer (no action — workflow-side bug is owner-gated), @atilcan65 (1-line fix to Auto-Verdict-By hook in label-check.yml — guard `context.payload.label?.name` OR add `if: github.event.action == 'labeled'`) |
| TD-038 | architect | Issue #739 (URL hygiene) delivered PR #749 covering `docs/decisions/*.md` only — 314 stale `atilcan65/AtilCalculator` refs remain across `scripts/` (20 occurrences / 7 files) + `.claude/` + `docs/` ex-decisions (294 occurrences / 30+ files); SAME root cause, OUT of Issue #739 scope (NEW) | Cycle ~#2110 post-PR #749 audit (2026-07-01T19:35Z): grep on `arch/issue-739-url-hygiene` branch counted remaining drift. **Issue #739 body scope was narrow**: "86 stale `atilcan65/AtilCalculator` URLs across 21 ADR files in `docs/decisions/`" — arch fixed exactly that scope (PR #749: 101 occurrences / 20 files / 75ins+75del pure rename). **Audit shows drift is 4x larger** than Issue #739 surface — Issue #739 was a CHORE that pre-identified the highest-visibility subset (ADRs are heavily cross-linked), but the underlying rename was never propagated to: (a) `scripts/` including 5 d-tests that grep for `atilcan65/AtilCalculator` (`d046`, `d095`, `d096`, `d105`, `proactive-sweep-test.sh`) — these tests still PIN the old slug, so they pass on main (greppability check matches old) but a future tester regression on "all d-tests must use canonical org slug" would fail; (b) `.claude/CLAUDE.md` lines reference `atilcan65/AtilCalculator` for owner-tagging + PR examples — still valid (owner IS @atilcan65), but cross-link to `atilproject/AtilCalculator` is missing on Issue #684, #598, etc; (c) `docs/sprints/sprint-NN/{plan,close,RETRO}.md` retrospective references — historical accuracy preserved, but new readers might infer the repo was always at atilproject; (d) `docs/backlog/STORY-S21-NNN.md` file ownership matrix entries; (e) `scripts/install/systemd/dev-studio-watcher@.service` template — instance path `$HOME/projects/AtilCalculator` is owner-machine-specific and unaffected, but the `REPO` env hint inside the service file is `atilcan65/AtilCalculator`. **URLs still resolve** via GH HTTP 200 redirect (no functional break — Issue #113 sister-pattern), so this is HYGIENE drift, not P0. **Decision**: split into SEPARATE sprints per file ownership matrix (each lane owns its own rename batch): arch lane = `docs/decisions/` (DONE PR #749); dev/test lane = `scripts/` (d-tests pin the old slug, must update first to avoid false-positive GREEN); PM lane = `docs/{product,backlog,sprints}/` (historical references OK, but new docs should use canonical); orch lane = `.claude/CLAUDE.md` doctrine file (owner-gated per file ownership matrix). **Per ADR-0015 + ADR-0031**: cannot expand PR #749 scope (single-PR discipline), cannot push to `.claude/` without owner gate. **Sister-pattern**: Issue #113 (scoped hygiene chore sets precedent for "do narrowly, follow-up widely"). **Operational impact**: zero today (URLs resolve, d-tests still pass on the old slug); 1-2 SP per lane for the cleanup; non-blocking but worth tracking. | PR #749 (MERGED pending owner squash, 101 occ / 20 files in `docs/decisions/`); audit grep on `arch/issue-739-url-hygiene` branch (cycle ~#2110, 2026-07-01T19:35Z); Issue #739 body scope ("21 ADR files"); 5 sister d-tests (`d046-expansion.sh`, `d095-post-org-migration-clone-urls.sh`, `d096-soul-files-template.sh`, `d105-audit-project-refs.sh`, `proactive-sweep-test.sh`) currently passing on stale slug | **L** (hygiene drift only; URLs resolve via GH HTTP 200 redirect; d-tests still pass on old slug; no functional break; 1-2 SP per lane for cleanup; non-blocking) | Sprint 23+ P2 (cluster P0 ships first); per-lane owned follow-ups: (a) arch lane = none remaining (PR #749 closed `docs/decisions/`); (b) dev/test lane = `scripts/` rename batch + d-test slug updates (~1 SP); (c) PM lane = `docs/{product,backlog,sprints}/` historical references audit (~0.5 SP); (d) orch lane = `.claude/CLAUDE.md` owner-gated (0.25 SP, owner squash). Trigger: when any sister d-test (`d046`/`d095`/`d096`/`d105`) starts failing on canonical-slug regression OR when a new d-test contracts "all d-tests must use canonical org slug" and 5+ tests fail. | @architect (filed + audit grep + per-lane ownership map), @developer (`scripts/` rename batch + d-test slug updates), @tester (d-test regression spec for canonical-slug greppability), @product-manager (`docs/{product,backlog,sprints}/` historical references audit), @orchestrator (`.claude/CLAUDE.md` owner-gated request), @atilcan65 (owner squash of orch lane follow-up + sprint prioritization decision) |
| TD-044 | architect | Sprint 22 PIVOT Faz 1.2 cascade — 2 P0 issues from PR #709 mpmath integration: CI infra gap (#727) + engine perf hotfix (#728) — supersedes TD-042 (NEW) | Cycle ~#1741 (2026-06-30T17:41:46Z) opened **Issue #727** [P0] initially framed as 'engine perf regression' (test_transcendental p99=189.56ms = 1.9× over 100ms, test_arithmetic p99=215.48ms = 4.3× over 50ms) — orchestrator discovered via PR #694 CI red (run 28463759088, job 84358086255, branch d5fdfcf5). **Cycle ~#1741 (17:42Z)** tester (cmt 4846307077) **RE-FRAMED** as **CI infrastructure gap**: `.github/workflows/ci.yml` Test (Python) step does NOT pass `BUDGET_MULTIPLIER` env var to self-hosted runner (PR #709 cascade — Sprint 22 PIVOT Faz 1.1 self-hosted runner migration missed this env var wiring). Fix Option B (5-min): add `env: BUDGET_MULTIPLIER: ${{ vars.BUDGET_MULTIPLIER || 1.0 }}` to ci.yml. **Cycle ~#1742 (17:43:25Z)** dev opened **Issue #728** [P0] filed as `agent:architect + needs-architect-review` — the actual engine-side architectural decision (separate from #727 CI infra gap). My cycle ~#1741 verdict on #727 was based on pre-reframe body (TD-007-class miss on pre-verdict cross-check, Issue #430 §Timing window); follow-up cmt 4846375794 acked reframe + approved Option B. My cycle ~#1742 verdict on #728 (cmt 4846373274) traced root cause to **`src/atilcalc/engine/evaluator.py:33` module-level `import mpmath`** adding ~150ms cold-start cost to ALL engine tests (not just transcendental). Arithmetic p99=215ms ≈ mpmath import (~150ms) + arithmetic compute (~50ms) — the overage is **import overhead**, not compute. **Recommended fix** (architect-approved, dev-implementing): lazy-import mpmath inside `_eval_transcendental()` helper, NOT at module level. `import` inside a function is cached in `sys.modules` after first call; arithmetic path never imports mpmath. **Sister**: d110 d-test (tester-owned, dev-implemented) verifies lazy-import behavior — mirrors d100 env-aware budget pattern. **Supersedes TD-042** (which was a generic 'ADR-0019 amend 3' ticket; this verdict crystallizes it as the lazy-import contract specifically). **Sister-pattern**: TD-016/018/020 (end-to-end contract blind-spot family — my PR #709 review missed mpmath import → cold-start → perf budget impact; lens (a) data flow + lens (b) runtime preconditions should have caught), TD-031 (rubber-stamp + L337 syntactic regression), TD-035 (heartbeat drift on tech-debt.md edit), TD-037 (workflow-side silent-skip preflight), TD-007 (architect posted review before reading peer's blockers — same class as TD-007 on #727 reframing). **Severity**: **M** (P0 contained; CI infra fix is 5-min, engine hotfix is ~2h; both well-scoped; sister-pattern to TD-015/016/018/020 — same-class lesson, 9-Lens blind-spot family 12th instance). **Payoff trigger**: (i) #727 ci.yml YAML edit lands (owner squash per file ownership matrix); (ii) #728 lazy-import PR merges with d110 d-test GREEN + ADR-0019 amendment 3 drafted; (iii) PR #694 squash-merge unblocked. **Architectural lesson (12th in 9-Lens blind-spot family, NEW)**: when adding a runtime dep to the engine (mpmath, scipy, etc.), the **module-level import location** is an architectural decision — eager import pays cost on every engine call (including non-using paths); lazy import pays cost only on demand. Per ADR-0017 §engine ↔ UI separation, the engine is a pure module with no I/O; module-level eager imports are an implicit API contract (they're paid by all importers). The mpmath import site should have been documented in ADR-0019 amendment 2 §2 as part of the lazy-vs-eager decision; future engine dep additions MUST include import-site rationale. | Issue #727 (P0, opened 2026-06-30T17:41:46Z, CI infra gap, tester reframed 17:42Z); Issue #728 (P0, opened 2026-06-30T17:43:25Z, agent:architect, engine perf hotfix); PR #694 (d093 d-test rebase, owner plate, CI FAIL); PR #709 (Sprint 22 PIVOT Faz 1.1, eb64485, source of mpmath integration + ci.yml gap); arch verdict cmt 4846358995 (Issue #727 initial, STALE); arch verdict cmt 4846373274 (Issue #728, lazy-import); arch follow-up cmt 4846375794 (Issue #727 corrected verdict); ADR-0019 amend 2 §2 (mpmath==1.3.0, mp.dps=50, <100ms transcendental); ADR-0017 §engine ↔ UI separation; ADR-0045 9-Lens pre-publish gate | **M** (P0 contained; CI infra fix 5-min + engine hotfix ~2h; 12th in 9-Lens family; supersedes TD-042) | Sprint 22 PIVOT P3 follow-up (cluster with TD-040/041/043): (1) #727 ci.yml YAML edit + repo var set (owner squash gate per file ownership matrix); (2) #728 lazy-import PR + d110 d-test (dev impl + tester TCs) + ADR-0019 amendment 3 (architect drafts); (3) PR #694 squash-merge unblocked once #727 + #728 land. | @architect (filed + Issue #727 + #728 verdicts + ADR-0019 amendment 3 design + d110 test contract), @tester (d110 d-test contract — workflow_dispatch + lazy-import behavior assertion + arithmetic p99 <50ms regression), @developer (Issue #727 ci.yml draft PR + Issue #728 lazy-import impl PR), @atilcan65 (Issue #727 YAML squash gate + Issue #728 merge gate per file ownership matrix + ADR-0019 amend 3 owner approval) |
| TD-045 | architect | Cross-user env-var pattern coverage gap — pre-PR #765, deploy scripts had no canonical doctrine for `vars.X` → workflow YAML default → script `$USER` fallback chain; RCA-17 surfaced it; ADR-0064 closes doctrinal gap (NEW) | Cycle ~#3349 (2026-07-03T22:38Z, REPRIME recovery + wake_nudge pickup) — RCA-17 LIVE INSTANCE (PR #764 RCA-17 AC4 fix **PENDING squash @ 8384ccb6 on branch `RCA-17-deploy-runner-ac4-user-fix`** per cycle ~#3363 F-6 re-attestation; NOT yet on main) + Issue #765 follow-up (deploy.yml env-var addition, owner-gated per file ownership matrix) + arch 9-Lens review (cycle #3316 cmt 4869829583) 🟢 OK + 1 architectural 🟡 (AC9 cross-user verdict) all converge on the same doctrinal gap: **no canonical ADR for cross-user env-var pattern**. Pattern emerged ad-hoc across RCA-16 (PR #358-era Sprint 6 P1 redesign, hardcoded `atilcan`) + RCA-17 (PR #764, `${ATC_SERVICE_USER:-$USER}` script-side fallback) + Issue #765 (proposed `vars.ATC_SERVICE_USER || 'atilcan'` workflow YAML default). Without doctrinal anchor, future engineer adding similar cross-user pattern (e.g., `ATC_LOG_DIR`, `ATC_CONFIG_OWNER`) has no reference; the gap could silently re-emerge. **Doctrinal fix**: [ADR-0064](./decisions/ADR-0064-cross-user-env-var-pattern.md) — 3-tier canonical precedence chain (vars.X repo var > workflow YAML hardcoded default > script-side `$USER` fallback) + 9-Lens attestation + **d121 d-test contract committed, impl deferred to Sprint 23 P2 follow-up PR (Path B per tester verdict cmt 4871079396 + F-5 cmt 4871123330, cycle ~#3359)** + **F-6 lens (j) re-attestation cycle ~#3363 per tester cmt 4871194663** (corrected PR #764 merge-claim hallucination in 8+ ADR lines + commit messageBody). **Doctrinal correction in cycle ~#3359**: env-var precedence sister-pattern family corrected from earlier "5 members d109/d110/d112/d113/d121" mis-citation to accurate "3 sisters shipped (d109/d112/d117) + d121 contract pending" — d113 is markdown-internal-links (unrelated lint); d110 is mpmath lazy-import (engine-side perf, not env-var). **Doctrinal correction in cycle ~#3363 (F-6)**: PR #764 status is **OPEN @ 8384ccb6 on orphan branch**, NOT merged to main; earlier "PR #764 MERGED @ 8d9540b" claim was hallucinated — 8d9540b is PR #762 squash commit (BUG #759 pct_change, unrelated); `origin/main @ 8d9540b` has 0 ATC_SERVICE_USER refs (verified `git show origin/main:scripts/deploy-runner.sh \| grep -c ATC_SERVICE_USER = 0`). **Sister-pattern family**: env-var precedence codified as canonical doctrine across 3 distinct families — operator-tunable perf-budget (ADR-0019-amend-4: BUDGET_MULTIPLIER/SUBPROCESS_TIMEOUT_S, fail-loud ValueError); runtime API hot-path gate (ADR-0019-amend-5: ATILCALC_EVALUATE_PERSIST, 2-tier boolean); **cross-user identity (ADR-0064: ATC_SERVICE_USER, 3-tier vars.X > workflow-default > script-fallback, fails clean)**. **Sister-pattern**: TD-016/TD-018/TD-019/TD-020/TD-028/TD-029/TD-030/TD-037 (architectural observability blind-spot family); TD-035 (heartbeat tight loop); TD-044 (engine import-site architectural decision sister — both are "where does the resolution happen?" questions); **TD-046-extension (this row's F-6 lesson is the 17th in 9-Lens blind-spot family — see cycle #3363 observation note)**. **Severity**: **L** (doctrinal gap only; PR #764 PENDING squash; Issue #765 owner-gated follow-up; no functional break today). **Payoff trigger**: when (i) ADR-0064 owner-squash merges (architect doctrine ratified, lens (j) verified at cycle ~#3363), (ii) **PR #764 actually merges to main** (was hallucinated as already merged in cycle #3349, re-attested cycle #3363), (iii) Issue #765 deploy.yml env block lands (owner squash), (iv) d121 d-test ships GREEN per ADR-0044 RED-first (Sprint 23 P2 follow-up PR, sister-pattern STORY-d121-d-test), (v) next sister cross-user env var (e.g., `ATC_LOG_DIR`) appears → ADR-0064 cited as doctrinal anchor. **Architectural lessons (2 in this row)**:
- **#13th in 9-Lens blind-spot family (cycle #3349)**: cross-component env-var patterns (workflow YAML ↔ script ↔ systemd user-session) span 3 ownership lanes. The pattern's **canonical precedence chain** is the doctrinal anchor; without it, each lane invents its own resolution and drift accumulates. Sister-pattern to TD-016 (data flow blind-spot) — cross-component traceability is a pre-publish gate item.
- **#17th in 9-Lens blind-spot family (cycle #3363 F-6)**: lens (j) live-state verification is **load-bearing** — claim of "PR #X MERGED @ <SHA>" must be verified via `gh pr view <X>` (state, mergedAt, mergedCommitSha) NOT inferred from git log tail (which shows the most recent merge, not the most recent PR merge). Cross-PR reference fabrications evade the standard 4-cat + verdict pre-publish gate because they look like docs artifacts but semantically make implementation claims that can't be verified until PR #X actually merges. Fix: when an ADR references a PR's state, the lens (j) attestation MUST include `gh pr view X --jq '{state, merged, merged_at, merged_commit_sha, head_ref, head_sha}'` ground truth, NOT git log tail. | RCA-17 (PR #764 + Issue #765 + arch 9-Lens cmt 4869829583); RCA-16 (PR #358-era Sprint 6 P1 redesign); [ADR-0064](./decisions/ADR-0064-cross-user-env-var-pattern.md) (this ADR closes the doctrinal gap; **lens (j) re-attested cycle #3363 per F-6**); ADR-0019-amend-4 + ADR-0019-amend-5 (env-var precedence family); ADR-0010 (systemd user-service); ADR-0030 (self-hosted runner user); ADR-0049 (d-test framework — d121 sister); ADR-0055 (Cadence Rule 1 atomic — this TD + ADR-0064 + INDEX.md row + d121 d-test contract in same PR-cluster, d121 impl deferred to follow-up PR per Cadence Rule 1 atomic cross-PR-cluster variant — sister-pattern to ADR-0060 deferral); **cycle-3363 observation note** (F-6 lens (j) re-attestation lesson, 17th in 9-Lens blind-spot family) | **L** (doctrinal gap only; PR #764 PENDING squash; Issue #765 owner-gated follow-up; non-blocking; d121 contract committed in ADR-0064, impl deferred to Sprint 23 P2 follow-up PR; lens (j) re-attested cycle #3363) | Sprint 23+ P2 (after Issue #765 owner squash + d121 GREEN follow-up PR + **PR #764 actually merges to main**). Owner applies `vars.ATC_SERVICE_USER || 'atilcan'` to `.github/workflows/deploy.yml` env block (~3 lines, human-only territory per file ownership matrix). **Owner squash gate for PR #764** (per ADR-0031) — currently open @ 8384ccb6 on orphan branch; cannot assume merge until `gh pr view 764 --jq .merged` returns true. Tester authors d121 regression (≥3 TCs RED-first per ADR-0049 baseline) in Sprint 23 P2 follow-up PR (Cadence Rule 1 atomic cross-PR-cluster variant). Optional: Sprint 24+ candidate — multi-tenant cross-user pattern generalization (e.g., `ATC_LOG_DIR`, `ATC_CONFIG_OWNER`). | @architect (filed + ADR-0064 authored + d121 test contract + 9-Lens attestation + cross-watchdog on Issue #765 + Issue #566/567 stale-FP RCA + Path B apply cycle ~#3359 + **F-6 lens (j) re-attestation cycle ~#3363**), @developer (d121 d-test impl in Sprint 23 P2 follow-up PR + cross-user pattern audit across `scripts/*.sh` per ADR-0064 §Out of scope + PR #764 squash to main per ADR-0031), @tester (d121 d-test sign-off per ADR-0044 RED-first, follow-up PR + F-6 escalation cmt 4871194663 + critical finding cmt 4871138817), @atilcan65 (Issue #765 deploy.yml env block squash gate + ADR-0064 owner-approval squash + **PR #764 owner-squash gate per ADR-0031** + workflow YAML approval per file ownership matrix) |

